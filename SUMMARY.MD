# 프로젝트 작업 요약 (다른 세션 인계용)

**작성일:** 2026-09-25 | **현재 버전:** v1.25.0 | **작업 폴더:** `C:\Users\dongbae\Desktop\Automatic_generation_of_approval_requests_html`

## 1. 프로젝트 개요

중학교 교사용 **"자동 품의 요구 생성기"** — 쇼핑몰 주문/장바구니 화면을 캡처(MHTML/HTML)하여 품목(품목명·규격·수량·예상단가·배송비)을 자동 추출하고, 서식 엑셀 `품목내역(통합).xls`에 누적 저장하는 Electron 데스크톱 앱.

- **배포 산출물:** `자동품의요구생성기_Portable.exe` (72MB, 작업폴더 루트) = `app\release\AutoRequisitionGenerator-Portable.exe` 사본
- **원본 소스:** `app\` 폴더 전체 (명세 MD 4종은 루트에 있으며 as-built로 갱신 완료: RPD/SYSTEM/ALGO/UIUX)

## 2. 현재 구현 기능 (v1.16.0)

| 기능 | 상태 |
|---|---|
| [📖 관리자 매뉴얼 PDF + 북마크바 Guest/System 프로필 지원 (v1.25.0)] ① **관리자 매뉴얼 신설**(analysis/make-admin-manual-pdf.js → resources/admin-manual.pdf 91KB, 보라 테마) — 도구 개요·사전 준비(API 키·Git 권한·저장소 루트 실행)·화면 구성(id 규칙: 주문서=입력값/장바구니=id-cart)·생성 절차·버전 관리(rules.json 신형식·patch bump·커밋 메시지)·오류 대처(503 재시도/폴백·id 중복 자동 덮어쓰기·push 실패·품질 개선). **관리자 창 헤더에 [📖 매뉴얼] 버튼 추가** — open-admin-manual IPC로 PDF 열기(패키지 exe는 resources→userData 복사 방식, 사용자 매뉴얼과 동일 패턴). 일반 사용자 매뉴얼과 완전 분리 ② **북마크바 추가에 Guest Profile·System Profile 포함** — listBrowserProfiles가 특수 프로필을 명시 제외하던 것을 폴더 존재 시 포함으로 변경(special 플래그, Bookmarks 파일 없어도 나열). 추가 시 **ensureBookmarksFile**이 Bookmarks 파일이 없는 특수 프로필에 Chrome 골격(root 3폴더·version 1)을 생성해 북마크릿 추가 가능. 실측: Chrome Guest/System Profile 모두 목록 반영 + 일반 프로필(동배·penz.kr 등)·Edge·Whale 무영향, Guest Profile은 Bookmarks 기존 존재 확인. 검증: E2E 19샘플 err 0 + adminCheck v1.0.2 synced + rendererCheck [] | ✅ v1.25.0 |
| [📖 매뉴얼 전면 갱신(관리자 내용 배제) + 다이소몰 무료배송 배송비 수정 (v1.24.0)] ① **사용 매뉴얼 전면 갱신**(make-manual-pdf.js → resources/manual.pdf 110KB) — 13몰 표기→34몰, 삭제된 시작화면 [등록] 버튼→[⭐ 북마크바 추가](프로필 선택), 확장명 "품의 생성기"→**"품의캡처"**, 확장 설치를 version2 자동 설치 도구 3단계로 전환(수동 설치는 부록), 장바구니 V체크·중복삭제·부분저장 경고·설정 메뉴(휠 줌 감도·글자 크기·시작 프로그램·보관 기간·규칙 업데이트)·매핑 학습·FAQ 신설. **관리자 전용 내용(F9·Gemini·규칙 생성/삭제 도구)은 매뉴얼에 탑재 금지 — 검증 스크립트로 CLEAN 확인** ② **다이소몰 장바구니 무료배송 배송비 버그 수정** — 원인: 페이지가 정책 안내 문구 '배송비: 3,000원 (30,000원 이상 무료)'를 실제 무료 여부와 무관하게 항상 표시하고 규칙이 이 문구의 금액을 그대로 추출. 수정: **extract.js selector 모드에 freeOver 지원 추가**(상품 합계 ≥ freeOver이면 배송비 행 미생성 — conditional 모드 제외로 기존 몰 무영향) + daisomall.json shipping에 `freeOver: 30000` 적용. 검증: 픽스처(합계 13,000→3,000원 유지) + 합성(30,000/39,000→null, 20,000→3,000) ALL PASS + test-cart-rules 13몰 ALL PASS + E2E 19샘플 err 0 + adminCheck v1.0.2 synced | ✅ v1.24.0 |
| [📦 rules.json 버전 관리 + 설정 버전 배너 + e2e 정리 (v1.23.0)] ① **rules.json 형식 전환** — 기존 규칙 배열 → `{version, generatedAt, count, rules:[...]}`. **구 배열 형식은 읽기에서 하위호환**(check-rule-updates가 둘 다 받음 — 원격이 구형식인 동안 구버전 앱도 정상, 신형식을 푸시하면 구버전 앱의 [업데이트 확인]은 형식 오류 표시) ② **버전 자동 증가** — 관리자 도구의 Git 자동 커밋/푸시 시(생성·삭제 모두) patch +0.0.1, `make-rules-json.js` 실행 시에도 +0.0.1 (doGit 해제 시 버전 유지). 현재 로컬 rules.json = **v1.0.1 · 규칙 34건**(aliexpress 주문서+장바구니 2건 추가 — v1.22.0 재시도 플로우로 성공 생성됨) ③ **설정 ⚙ 상단 큼직한 버전 배너** — 보라 테두리 박스에 `v1.0.1`(26px 복드림) + 생성 일시(ko-KR) + 규칙 수, 서버 최신 버전도 [업데이트 확인] 후 표시(remoteVersion). 버전은 rules-version IPC로 저장소 루트 rules.json에서 읽음(포터블 exe는 exe 위치 기준 탐색) ④ 관리자 도구 생성·삭제 완료 시 설정 배너 즉시 갱신 ⑤ **루트 e2e*.* 28개 파일 삭제**(테스트 산출물 — E2E 실행 시 e2e-result.json만 재생성됨). 검증: E2E 19샘플 err 0 + adminCheck{ok, rules 34, version 1.0.1, rulesJsonSynced true} + rendererCheck [] | ✅ v1.23.0 |
| [🛡 관리자 도구 Gemini 503 대응 + 재시도 충돌 수정 (v1.22.0)] 실측(2026-09-25): Gemini 3.1 Flash Lite 503 High Demand 발생 + 503로 생성 실패한 뒤 재실행 시 "규칙 id ~가 이미 있습니다"로 막힘(주문서 규칙은 저장 완료 후 장바구니 규칙에서 503 → 부분 저장 상태로 실패). ① **지수 백오프 재시도** — 429·500·503·504·네트워크 오류에 2초→4초→8초 간격 최대 3회 자동 재시도(로그에 실시간 표시) ② **모델 자동 폴백** — 재시도 3회 모두 실패 시 gemini-2.5-flash → gemini-2.0-flash 순으로 자동 전환 후 동일 재시도 ③ **트래픽 안내 팝업** — 전부 실패하면 "현재 구글 서버 트래픽이 많으니 잠시 후 다시 시도하거나 모델을 변경해주세요" alertBox 표시 ④ **재실행 충돌 근본 수정** — 충돌 검사가 notes에 '관리자 도구 자동 생성' 마커가 있는 규칙은 **자동 덮어쓰기**(재시도 시 수동 삭제 불필요), 매핑 등 사람이 만든 규칙만 차단하며 오류에 충돌 파일 전체 경로 표시. 실패 잔재(%APPDATA%\자동 품의 요구 생성기\rules\aliexpress.json — 사용자 삭제 시도에도 잔존했던 것) 대신 정리 완료. 검증: E2E 19샘플 err 0 + adminCheck ok(rules 32·synced) + rendererCheck [] | ✅ v1.22.0 |
| [🛡 관리자 도구 4건 개선 (v1.21.0)] ① Gemini 모델 기본값 **Gemini 3.1 Flash Lite**(3.1 flash/pro 추가) + 모델·API 키 **[💾 불러오기]** 버튼(settings.json 재로드) ② **주문서/장바구니 선택적 생성** — 파일을 넣은 종류만 규칙 생성(주문서만/장바구니만/둘 다), id 규칙: 주문서=입력값(예: musinsa), 장바구니=주문서 id+'-cart'(musinsa-cart) — 입력이 -cart로 끝나면 자동 벗겨 베이스로 사용, 프롬프트도 종류별 분기(장바구니=checkedOnly 필수·perFee 안내·예시 gmarket-cart/naver-cart/coupang, 주문서=checkedOnly 금지·예시 gmarket/emartmall/dreamdepot-order), 장바구니 규칙에 checkedOnly 누락 시 경고 로그 ③ **샘플 파일 종류별 최대 2개**(HTML+MHTML 혼합 첨부 — 다중 선택, 첨부분 모두 프롬프트에 샘플로 투입) ④ **구축된 쇼핑몰 목록·삭제** — 샘플 파일 섹션 바로 아래 [▼ 목록 보기] → builtin 우선순위 순 몰 목록(id·이름) → 2단계 확인(예/아니오) 후 삭제: 소스 규칙+analysis 사본+사용자 사본 동시 삭제 → **rules.json 재반영**(make-rules-json.js도 ORDER 누락 throw→skip으로 변경해 삭제된 몰 허용) → Git 체크 시 `chore: {id} 쇼핑몰 규칙 삭제 및 rules.json 갱신` 커밋·푸시. 신규 규칙 생성 시 analysis/rules 사본도 동기 저장. IPC: admin-list-malls·admin-delete-mall 신설, admin-pick-file은 다중 선택 배열 반환으로 변경. 검증: E2E 19샘플 err 0 + adminCheck ok(rules 32·rulesJsonSynced true) + rendererCheck [] | ✅ v1.21.0 |
| [🛡 관리자 원클릭 규칙 생성 (ADMIN.md)] 설정 ⚙ 화면에서 **F9** → 관리자 모달: Gemini API 키(settings.json 저장/로드)·모델 선택(gemini-2.5-flash/pro/lite·2.0·1.5)·쇼핑몰 이름(한글)·규칙 id(이름에서 자동 파생·수정 가능)·샘플 3종 피커(장바구니/주문서 HTML·MHTML + 정답 엑셀 .xls/.xlsx) → [🚀 쇼핑몰 규칙 생성 및 자동 배포 시작] ① 샘플 HTML 추출(script/style/svg 제거·공백 정리·160KB 절단, MHTML은 루트 text/html 디코딩) + 정답 엑셀 파싱(loadExcelFull 헤더 자동탐지) + 내장 규칙 3종(gmarket-cart·naver-cart·emartmall) few-shot → Gemini REST generateContent(responseMimeType:json, 2.5 계열은 thinking off, 180s 타임아웃) ② 응답 JSON 추출·검증(id 형식/match 배열/rowSelector/fields.name·price) ③ **소스 규칙**(app/src/main/lib/rules/{id}.json — git 배포용) + **사용자 규칙 사본**(userData/rules — 실행 중 앱에 즉시 적용) 동시 저장 → **rules.json 재생성**(make-rules-json 로직을 lib/admin.js에 인라인 — 패키지 exe엔 시스템 node가 없어 spawn 불가) ④ git add(규칙 파일+rules.json 2파일 한정 — `git add .` 대신)·commit(`feat: {몰} 쇼핑몰 규칙 추가 및 rules.json 갱신`)·push. 진행 로그는 모달 콘솔에 실시간 표시(`admin-log` IPC). repo root 탐지: PORTABLE_EXECUTABLE_DIR → app 상위로 rules.json+.git 검색(포터블 exe도 저장소 루트에서 실행하면 동작). **+ 기존 버그 수정: index.js serveDoc의 `smartDecode` import 누락**(뷰어 CSS 파트 서빙 시 ReferenceError → cssPartCheck ERR — v1.19.1 베이스라인 E2E에서도 재현 확인) — import 추가로 cssPartCheck CSS 7파트 200 응답 복구. 검증: E2E 19샘플 err 0 + **adminCheck 신설**({ok, rules 32, rulesJsonSynced true}) + rendererCheck [] + mappingCheck 정상 | ✅ v1.20.0 |
| [🧩 규칙 업데이트 서버 연결(github) + 최신 상태 표시] ① **깃허브 저장소에 rules.json 탑재** — repo parkdongbae-afk/AutoRequisitionGenerator main 루트에 전체 32몰 최신 규칙 배열을 단일 파일로 커밋·푸시(bf49c5d), 업데이트 URL 기본값 = raw.githubusercontent.com/.../main/rules.json ② 설정의 **[⬇ 업데이트 확인]**이 실제 주소로 동작 ③ **최신 상태 표시**: 설정을 열면 저장된 주소로 자동 확인(조용히)하고 결과 배지 표시 — ✅ 최신 / ⬆ 갱신 가능(규칙 이름 나열) / ⚠ 실패(사유), 마지막 확인 시각 포함, settings ruleUpdateLastCheck으로 저장(재시작 유지). **주의: 저장소가 비공개면 익명 다운로드가 404** — 규칙 업데이트를 받으려면 저장소를 공개(Public)로 전환해야 함(2026-09-25 실측: 비공개 404 확인). 검증: rules.json 32건 파싱·id 중복 없음 + 푸시 완료 + E2E 19샘플 err 0 | ✅ v1.19.1 |
| [📥 inbox 자동 정리 + 깃허브 규칙 업데이트] ① **inbox 보관기간 자동 삭제** — 프로그램 시작 시 inbox에서 보관기간 경과 캡처(.mhtml/.html/.htm)+sidecar(.url.txt) 삭제, 설정 `inboxRetentionDays`(기본 **5일**, 0=끔, 0~365)로 사용자 조정, 설정 ⚙에 입력란 추가(알고리즘 독립 검증: 7일/6일 파일+sidecar 삭제·1일/최신 보존 PASS) ② **깃허브 규칙 업데이트** — 설정 ⚙에 업데이트 주소 입력란+**[⬇ 업데이트 확인]** 버튼: `check-rule-updates` IPC가 주소의 규칙 JSON(**규칙 객체 배열**)을 net.fetch로 내려받아 같은 id 규칙과 안정적 비교(키 정렬 직렬화) → 변경분만 **사용자 규칙으로 저장**, allRules가 **builtin 자리에서 교체**(뒤에 붙이면 naver→naver-cart 매칭 우선순위 깨짐 방지) — **네이버·쿠팡 장바구니 포함 모든 내장 규칙 업데이트 가능**(같은 id로 올리면 됨). 새 규칙도 배열에 추가하면 신규 등록. 설정에서 `rulesUpdateUrl` 저장. 검증: E2E 19샘플 err 0(병합 변경 후 전 규칙 매칭 정상) + inbox 정리 검증 PASS | ✅ v1.19.0 |
| [🐞 북마크릿 네이버 축소 미복원 수정] 북마크릿 네이버 장바구니 캡처에서 **25% 축소 후 원래 배율로 돌아오지 않던 것** 수정 — zoom 스타일 태그를 페이지에 추가만 하고 제거하지 않았음. 직렬화+전송 직후 `zt.remove()`로 라이브 페이지 복원(저장 본문에는 태그가 포함된 채 전송되어 레이아웃 유지 — docstore가 뷰에서만 제거). 검증: 생성 코드에 제거 호출 포함 + 개행 무 + 전체 스위트 PASS | ✅ v1.18.4 |
| [🛒 북마크릿 네이버 장바구니 지원 + version2 도구 UI 앱 테마 적용] ① **북마크릿으로도 네이버 장바구니 추출**(사용자 요구 — 채널 제한 폐지): bookmarklet이 네이버 장바구니(shopping.naver.com/cart)에서 **25% 축소 후 캡처**하는 분기 추가(확장과 동일한 가상화 대응 — zoom 태그 주입→1000ms→직렬화, 앱 docstore가 뷰에서 태그 제거) + store의 naver-cart 채널 게이트·NAVER_CART_CHANNEL_MESSAGE 제거(부분 저장은 countMismatch 경고로 처리), naver-cart notes 갱신 + analysis 사본 동기화 ② **version2 설치 도구(ExtensionDeveloperModeManager) UI를 메인 프로그램 팔레트로**: tkinter 색상 매핑 — BLUE #2563EB·#4F46E5→**#5B4DFB**, hover #1D4ED8·#4338CA·#3730A3→**#4C3DE6**, 틴트 #EEF2FF→**#EEEDFE**, 배경 #F3F6FA→**#F4F6F8**, 보더 #DCE3EC→**#E2E8F0**, 헤더 타이틀 fg→보라. extension_auto 원본 수정 → run-extension-v2가 복제하는 extension_v2·패키징 resources 모두 반영. 북마크릿 코드 변경분은 **[북마크바 추가] 재실행으로 기존 북마크 URL 교체**(guid 보존). 검증: test-cart-rules ALL PASS + test-bookmarklet + E2E 19샘플 err 0(receiverCheck.bookmarkInfo true) | ✅ v1.18.3 |
| [🛠 3건 UX 다듬기: 프로필 기본 미선택·배송비 중복삭제 제외·익스텐션 추가 즉시 실행] ① 북마크바 추가 모달의 프로필 **기본값을 전체 미선택**으로 변경(사용자가 추가할 프로필을 직접 선택 — 브라우저 그룹 [전체선택] 토글은 유지) ② 추출결과의 **배송비 행은 중복삭제 대상 제외**(Grid 중복 감지 루프에서 isShipping 스킵 — 같은 장바구니 재추출 시 배송비 행에 버튼이 붙지 않음) ③ [🧩 익스텐션 추가] 버튼이 안내 창을 거치지 않고 **version2 설치 도구를 바로 실행**(store.runExtensionV2Flow — 도구 창에서 브라우저·프로필을 고르므로 중간 안내 불필요, 사용자 지정). 검증: E2E 19샘플 err 0 · rendererCheck [] | ✅ v1.18.2 |
| [⭐ 북마크바 추가 프로필 선택 + 네이버 원본 뷰 공백 수정] ① **북마크바 추가에 브라우저·프로필 선택 UI**(사용자 요구 — 확장 자동 설치 도구 창과 같은 패턴): bookmarks.js에 `listBrowserProfiles()` 신설(Local State profile.info_cache에서 표시명 읽고, Bookmarks 파일 있는 프로필만), `list-browser-profiles` IPC 신설, **add-bookmarklets에 selection [{browser,dir}] 매개변수**(지정 시 선택 프로필에만 추가 — 중복감지/종료확인/재시작 전부 선택 대상 한정), 신규 **BookmarkProfileModal**(브라우저 그룹 헤더+전체선택, 프로필 체크박스, 기본값=모든 브라우저의 모든 프로필 전체선택), Header [북마크바 추가]가 모달을 열도록 변경. 실측: Chrome 4프로필+Edge 1프로필 표시명 정상 ② **네이버 원본 뷰 공백**: 1.6.1의 saveAsMHTML 분기가 간헐 실패(실측 — 동일 세션 08:22 성공/08:42 실패)하면 CSS-zoom 폴백 문서가 되어 공백화 → (a) 확장(1.6.2) saveAsMHTML **3회 재시도** (b) docstore가 뷰용 html에서 arge-zoom 스타일 태그 제거(rawHtml·추출 무영향) (c) 뷰어에 **와이드 문서 자동 맞춤**(문서 폭 > 컨테이너면 첫 측정 시 폭에 맞춰 1회 자동 축소 — 문서당 1회). 검증: E2E 19샘플 err 0 + rendererCheck [] + mappingCheck 정상 | ✅ v1.18.1 |
| [🎨 네이버·쿠팡 뷰어 카드 레이아웃 전환] 원본 MHTML 렌더링이 깨지는 몰에 한해(사용자 지정: **naver-cart·coupang만**, 다른 몰은 원본 그대로) 뷰어를 추출 데이터 기반 **커스텀 카드 레이아웃**(네이버 쇼핑 장바구니 유사)으로 표시: ① 엔진에 품목 대표 이미지 추출 추가 — `rule.image {sel, attr}` 우선, 없으면 행 내 첫 유효 img(src→data-src 순, 로고/아이콘/스피너 제외, data:image는 500B 초과만) ② docstore rows에 image 전달 — **MHTML 파트 매칭 시 app-mhtml://로 해석**(오프라인 안전), 없으면 절대 https URL(loadDoc·reextract 양경로) ③ 뷰어: 좌측 88×88 썸네일(object-cover, 로드 실패 시 🛒 플레이스홀더) + 품목명·규격·수량 배지·올림 단가 우측 정렬, 배송비는 그룹 직후 점선 슬림 행 — 툴바에 [카드/원본] 토글(매핑 피커 모드에선 원본 강제). 다른 몰·주문서·엑셀뷰는 기존과 동일. 검증: 네이버(shop-phinf.pstatic.net)·쿠팡(thumbnail.coupangcdn.com 120x120) 이미지 URL 추출 실측 + test-cart-rules ALL PASS + E2E 19샘플 err 0(rendererCheck·mappingCheck 포함) | ✅ v1.18.0 |
| [🛠 네이버·쿠팡 캡처를 검증된 Chrome MHTML 경로로 전환 + 배송비 행 위치 개선] ① **네이버 장바구니(확장 1.6.1)**: CSS zoom 방식(1.5.9 레이아웃 붕괴/1.6.0 뷰어 공백 문제)을 버리고 **사용자가 검증한 플로우를 확장이 그대로 수행** — `chrome.tabs.setZoom(0.25)`(브라우저 확대/축소 — 가상화 목록 전체 렌더) → 1000ms → `pageCapture.saveAsMHTML`(리소스 전부 내장) → 줌 복원 → /mhtml 전송. Chrome 직렬화라 뷰어가 수동 저장과 동일하게 렌더링(스탬프·채널 메타는 saveAsMHTML 전 executeScript로 DOM에 박제되어 함께 저장됨), 실패 시 기존 라이브 경로 폴백 ② **쿠팡**: 사용자 지정 — Chrome MHTML이 뷰어 렌더링에 검증됐으므로 coupang.com도 **saveAsMHTML 우선** 경로로 전환(Referer 수정은 타 CDN 대비로 유지) ③ **배송비 행 위치**: perFee 배송비를 목록 끝이 아니라 **그 그룹 마지막 품목 바로 다음**에 삽입 — extract.js에 문서 순서 맵(docOrder)으로 품목·배송비 요소 위치 비교 후 병합(네이버: 라익미 2건→2,500 … 마미손 설거지→3,000 / G마켓: 그룹별 3,000 각 위치, 실측 확인). 검증: test-cart-rules ALL PASS(13몰) + test-cart-fix·feedback·stamped + E2E 19샘플 err 0 + channelCheck·refererCheck 양성 | ✅ v1.17.2 |
| [🎨 네이버 뷰어 축소 상태 저장 유지 + 쿠팡 캡처 CSS 누락 수정] ① **확장(1.6.0)** — 네이버 장바구니 캡처에서 줌 스타일 태그를 이전엔 제거했으나(뷰어 원래 크기 취지) **사용자 실측: 제거하면 뷰어 레이아웃이 무너지고 축소 상태 그대로 저장하면 정확히 보임**(가상화 레이아웃이 25% 계산 기준으로 박제되기 때문) → 줌 태그를 캡처에 **그대로 유지**(뷰어에서 휠 줌으로 확대), 축소는 네이버 장바구니 전용이라 타 몰 무영향 ② **mhtmlsave Referer** — 쿠팡 캡처가 뷰어에서 "HTML만 저장"처럼 깨져 보이던 원인: `assets.coupangcdn.com` CSS는 **Referer 없는 요청을 403 거부**(실측: 무헤더 403 / Referer: 출처페이지 200, 이미지는 무관) → fetchAsset이 출처 페이지를 Referer+Chrome UA로 요청하고, 실패 시 net.request로 Referer 강제 재시도 ③ **E2E refererCheck 신설** — buildMhtmlFromHtml이 쿠팡 CSS를 실제로 담는지 검증(외부 네트워크 막힌 환경은 skip, 실측 `{cssParts:1, ok:true}`). 검증: test-cart-rules ALL PASS + E2E 19샘플 err 0 + channelCheck·refererCheck 양성 | ✅ v1.17.1 |
| [🛠 네이버 장바구니 그룹별 배송비 분리 + 자동 스크롤 수집 전면 제거(확장 1.5.9)] ① **naver-cart 배송비 perFee 전환** — 기존 규칙이 하단 예상금액 패널 '총 배송비 +5,500원'(전체 합산)을 잡아 2,500+3,000이 **1식 5,500원으로 합쳐지던 것** 수정: 판매자 그룹별 '총 배송비 N원'(price_area, '도움말' 낀 표시 허용)을 perFee로 **각각 별도 행** 추출 — 무료 그룹 제외, 하단 패널은 '+'가 정규식 차단으로 자연 배제. 사용자 신규 픽스처(v_check_ok/장바구니_네이버.mhtml — 확장 캡처, 채널 메타 포함)로 품목 8건+배송비 2행 정답(.xls/.xlsx)과 정확 일치, **cart_03 케이스 재등록** ② **확장(1.5.9) 자동 스크롤·스티치 제거** — G마켓 등에서 장바구니 화면이 스스로 아래로 내려가는 것이 보였던 '스크롤 구간 행 수집+data-arge-rows 스티치'(구 가상화 방어)를 전 몰에서 삭제(사용자 요구 2026-09-25: 불필요) — 남는 것: 축소 25%(네이버 장바구니 전용)+1000ms, lazy 이미지 src 교체, checked 스탬프, 채널 메타. 캡처가 즉시 완료되어 체감 속도 향상. 검증: test-cart-rules ALL PASS(13몰·cart_03 포함) + test-cart-fix + test-feedback + test-cart-stamped + E2E 19샘플 err 0 + channelCheck 양성 | ✅ v1.17.0 |
| [🚨 네이버 장바구니 전면 거부 진범 수정 + G마켓 규격 원문 반영] ① **captureChannel 누락(진범)**: v1.16.0의 채널 검사(store.js `doc.captureChannel !== 'extension'`)가 처음부터 깨져 있었음 — docstore `summarize()` 반환값에 **captureChannel 필드가 없어** 렌더러가 받는 문서는 항상 undefined → **확장 캡처까지 포함해 네이버 장바구니 전부 거부**(v1.16.1·v1.16.2의 확장측 수정은 유효하나 이 버그를 못 넘음). summarize에 captureChannel 추가 + **E2E channelCheck 신설**(메타 있음→extension 통과/없음→null 거부/naver-cart 규칙 매칭 — 실제 loadDocument 경로 검증, 이번 사고 재발 감시) ② **G마켓 장바구니 규격 = 옵션 원문**: 규칙 스키마 신규 `specFromOption` — deriveSpec 패턴 추출(45cm 일부/빈칸) 대신 옵션 원문 전체를 규격으로 사용(gmarket-cart 적용). 같은 카드의 유닛들이 규격 빈칸으로 중복 품목처럼 보이던 것 해소. extract.js items에 spec 탑재 + docstore loadDoc·reextract 양경로 반영 ③ **cart_05 정답 재작성**: 사용자가 v_check_not_ok의 G마켓 픽스처를 교체(폴더 gitignore로 옛본 복구 불가)했으나 새 정답(9,500/2,800/2,600·배송비 0)은 캡처에 없는 값(캡처엔 3,990/1,990/16,000만 존재, 3,990−2,000(옵션Δ)=1,990·16,000÷10=1,600·배송비 2×3,000 검증) — 캡처 기준 정답(.xls) 재생성으로 회귀 유지(사용자 .xlsx 원본 보존). 라이브 카트 가격이 다르면 캡처 재업로드 시 정답 재조정 필요. 검증: test-cart-rules ALL PASS(13몰) + test-cart-fix + test-feedback + test-cart-stamped + E2E 19샘플 err 0 + channelCheck 양성 | ✅ v1.16.3 |
| [🛠 4건 피드백: 네이버 채널 오탐 재발 대응(확장 1.5.8)·축소 네이버 전용·옥션 단가·xlsx 서식] ① **확장(1.5.8) 채널 표식 조기 삽입** — v1.16.1에서도 재설치 후 오탐 거부 지속 → 앱 수신부(cheerio·charset 재기록)는 메타 보존 확인(실측), 원인은 captureLiveHtml이 네이버 장바구니의 거대 DOM 스크롤·스티치 중 예외로 중단되면 **맨 끝(288행)에 있던 메타 삽입 미도달 → 표식 없는 폴백 outerHTML 전송**. 메타 삽입을 ①캡처 최상단 ②저장 직전 ③catch 폴백 직전 ④syncCheckStates(saveAsMHTML 폴백에도 잔존) 4곳에 배치 + `arge-ext-version` 메타 추가(캡처에 확장 버전 기록) ② **25% 축소를 네이버 장바구니(shopping.naver.com/cart) 전용으로 한정** — 다른 몰·주문서는 축소·1000ms 대기 없이 원래 배율로 캡처(사용자 지정: 다른 쇼핑몰은 원래대로, 주문서는 축소 불필요) ③ **auction-cart 단가 버그** — box__item-price는 합계(실측: 마이크로비트 점퍼홀더 3개 6,780=2,260×3)인데 단가로 쓰고 있었음 → `priceIs: lineTotal` 추가(사용자 정답 .xlsx 수정본과 일치), notes 갱신 + analysis 사본 동기화 ④ **xlsx 서식 지원** — excel.js bookTypeOf(확장자 분기: .xlsx→xlsx, 아니면 biff8 — .xlsx 경로에 biff8 쓰면 Excel이 "확장자 잘못됨"으로 거부), save-excel-as 다이얼로그 .xlsx 필터 추가 + 확장자 판정 수정(기존 `.xls` 정규식이 `.xlsx` 미매칭 → 무조건 .xls 붙임) ⑤ test-cart-rules 정답 파일 .xls→.xlsx 폴백(사용자 갱신 정답이 xlsx 노트북에서 .xlsx로 저장됨). 검증: test-cart-rules ALL PASS(cart_09 옥션 통과 — 3×2,260·배송비 2,700 정답 일치, 체크미보존 수동저장 extras는 기존대로 무시) + test-cart-fix 36/36 + test-feedback 23/23 + E2E 19샘플 err 0 | ✅ v1.16.2 |
| [🐞 네이버 장바구니 V체크 있어도 "V체크된 상품이 없습니다" 거부(오탐) 수정] ① **원인(실증)**: 사용자 브라우저의 확장이 v1.16 이전 본이라 캡처에 `arge-channel=extension` 메타가 없었고 → v1.16.0의 채널 검사(store.js `captureChannel !== 'extension'`)가 naver-cart를 거부 → 여기에 `RULE_REJECT_MESSAGES`의 `'naver-cart'` 키 **3개 중복 정의**(객체 리터럴 마지막이 이김)로 채널 거부조차 'V체크 없음' 메시지로 표시. 추출 규칙 자체는 정상 — inbox 잔존 네이버 캡처 5개(19-12-47/19-13-12/19-13-27/19-50-33/19-52-04, 모두 메타 없음)에 현재 규칙 적용 시 2~4건 정상 추출 확인. 확장 복제 원천인 루트 `extension_auto/background.js`에 v1.16 코드(메타 삽입)가 누락된 구본(15,309B)이 남아 구버전 확장이 계속 유포되는 구조였음 ② **수정**: store.js 중복 키 정리(0건 거부에만 V체크 메시지) + 채널 거부 전용 `NAVER_CART_CHANNEL_MESSAGE`("아이콘으로 캡처했는데도 이 창이 뜨면 확장이 오래됨 → 브라우저 완전 재시작 / 익스텐션 자동 설치 재실행") ③ **재발 방지**: `run-extension-v2`가 extension_v2 폴더의 확장 코드를 항상 현재 `extensionFolder()`본으로 덮어쓰기 + 루트 extension_auto 내장 확장 코드 최신 동기화 + **확장 버전 1.5.7**(chrome://extensions에서 1.5.6 표시=구버전 즉시 식별). 사용자 조치: 새 exe 실행 후 **브라우저 완전 종료→재시작**(unpacked 확장은 브라우저 재시작 시 코드 리로드), 필요 시 익스텐션 자동 설치 재실행. 검증: test-cart-fix 36/36 + test-feedback 23/23 + test-cart-rules(cart_09 옥션은 기존 픽스처 문제 — 정답이 .xlsx인데 .xls 탐색, 본 수정 무관) + E2E 19샘플 err 0 | ✅ v1.16.1 |
| [🧪 채널별 네이버 동작 + G마켓 배송비 개별 행] ① **네이버 채널 구분**: 캡처 본문에 채널 표식 meta(arge-channel) 삽입(확장=extension, 북마크릿=bookmarklet) — 네이버 장바구니는 **확장 품의캡처 아이콘만 정상 추출**(추가상품 라인 분리 유닛 추출 복원, 부분저장 감지 유지), **북마크바 품의캡처/수동 저장은 '주문서 상태에서 품의캡처 눌러 주세요.' 팝업으로 거부**(store에서 captureChannel 검사) ② **G마켓 배송비 개별 행**: shipping 스키마에 perFee 추가 — 그룹별 배송비(3,000원×2 등)를 1식 6,000원 합산하지 않고 **배송비 3,000원×1씩 별도 행**으로 추출(엑셀 저장 시 금액별 집계는 기존대로). G마켓만 적용해 타 몰 회귀 최소화 ③ v1.15.0의 version2 실행기 경로 수정·익스텐션 모달 자동 설치 전용화·확장명 품의캡처 유지. 검증: test-cart-fix 36/36(네이버 부분저장 감지 복원·G마켓 배송비 개별 행) + test-feedback 23/23(checkedScope perFee 반영) + test-emart 8/8 + test-cart-rules 12몰 + E2E 19샘플 err 0 | ✅ v1.16.0 |
| [🧩 익스텐션 자동 설치 전용화 + 네이버 주문서 전용 + 확장명 품의캡처] ① **익스텐션 추가 모달 전면 재작성**: 제목 '익스텐션 수동 설치 안내' → **'익스텐션 자동 설치 안내'** — version1 수동 설치 안내(브라우저별 6단계·공통 준비·설치 후 확인) 전부 삭제하고 version2 자동 설치(도구 실행 버튼 3단계 안내)만 남김 ② 확장명 변경: manifest name '품의 생성기 - 주문화면 전송 (Chrome/Edge/Whale)' → **'품의캡처'**(action.title 동일) — 설치 도구가 자동 설치하는 확장 카드 이름도 품의캡처로 표시, 확장 아이콘 클릭 시 북마크 품의캡처와 동일(캡처 전송) 동작 ③ **네이버 주문서 전용 전환**: naver-cart 규칙을 거부 전용(rowSelector 매칭 없음)으로 전환 — 네이버 장바구니에서 품의캡처 시 0건 거부 + **'주문서 상태에서 눌러 주세요.'** 팝업(store RULE_REJECT_MESSAGES, naver 기존 'V버튼을 눌러~' 메시지도 동일 문구로 교체) ④ **version2 실행기 확장 경로 프리셋**: 설치 도구(ExtensionDeveloperModeManager)의 config.json(%APPDATA%\\ExtensionDeveloperModeManager)에 캡처 확장 폴더(%APPDATA%\\자동 품의 요구 생성기\\extension)를 extension_path로 미리 기록 — 도구 실행 후 폴더 선택 없이 버튼만으로 최신 캡처 확장이 자동 설치됨(앱 시작 시 자동 갱신되는 폴더라 항상 최신). 검증: test-emart 8/8 + test-cart-fix 34/34(네이버 거부 전용 2건) + test-feedback 23/23 + test-cart-rules 12몰(cart_03 제거 — 네이버 카트 추출 철회) + E2E 19샘플 err 0 | ✅ v1.15.0 |
| [🛠 5건 피드백: version2 경로 수정·G마켓 감지 제거·e마트몰 주문서 전용·네이버 축소 1000ms·뷰어 하단 잘림] ① version2 실행기 경로 수정 — extraResources from이 app 기준이라 루트의 extension_auto가 패키징에 누락되어 실행 실패(Temp/resources에 폴더 부재 실측) → from '../extension_auto'로 수정 후 resources 포함 확인, dev 경로도 app 상위로 수정 ② G마켓: 하단 계산기 '상품수 N개'는 추가상품 라인 등으로 체크/추출 수와 어긋날 수 있다는 사용자 확인 → verifyCount 감지 제거(경고 미발생) ③ e마트몰 주문서 전용 전환: 장바구니 규칙을 거부 전용(rowSelector 매칭 없음)으로 전환하고 RULE_REJECT_MESSAGES에 '주문서 상태에서 눌러 주세요.' 추가 — 장바구니에서 품의캡처 시 안내 팝업, 주문서에서만 추출(mhtml/html 모두 0건 확인). 추천상품 배제·배송비·V체크 로직은 주문서 규칙에 귀속 ④ 네이버 캡처 축소를 25% !important 스타일 태그 방식으로 강화(인라인 zoom이 재렌더에 묻히는 문제 대응) — 적용 후 **1000ms 대기**(사용자 지정) → 스크롤·수집 → 스티치·스탬프 → **축소 상태로 outerHTML 저장** → 즉시 복원 + 캡처본에서 줌 스타일 태그 제거. lazy 이미지 src 교체 추가(data-src 등 → 실제 src, 뷰어 하단 잘림 방지) ⑤ 뷰어 measure 스크립트에 600ms 주기 재보고 추가 — 이미지 로딩 등 비동기 높이 변화를 sizer에 반영해 하단 잘림 해소. 검증: test-emart 8/8(주문서 바나나 갱신·장바구니 거부 전용) + test-cart-fix 36/36 + test-feedback 23/23 + test-cart-rules 13몰(cart_03 새 정답·KNOWN_EXTRAS 설거지) + E2E 19샘플 err 0 | ✅ v1.14.0 |
| [🧩 version2 확장 실행기 + 네이버 추가상품 라인 분리 + e마트몰 V체크 배제/배송비 + 슬림 스크롤 캡처] ① **extension_auto(version2) 실행기**: 익스텐션 추가 모달 최상단에 [🧪 version2 확장 실행] 버튼 — extension_auto 폴더(ExtensionDeveloperModeManager v18.1, UI Automation으로 브라우저 종료→개발자 모드→압축해제 익스텐션 자동 로드)를 userData/extension_v2로 복제 후 install_and_run.bat 실행(IPC run-extension-v2, extraResources에 extension_auto 포함). version1 확장(기존 앱 데이터 폴더)은 그대로 유지. extension_auto 루트에 최신 확장 코드(manifest.json 1.5.5 + background.js)를 복제해 두어 도구로 이 폴더를 선택 설치하면 아이콘 클릭 시 캡처 전송 동작 ② **네이버 추가상품 라인 분리**: 한 상품 카드 안에 '추가상품' 라인이 여러 개면(div[class^=product_item--] 복수 — 본품+추가상품 각각 옵션/수량/라인가격 보유) units 스키마로 라인별 독립 품목 추출(예: 남성 특대형 고무장갑 = 마미손질긴 L 1@23,900 + 블랙 290mm 1@45,000). option regex '추가상품 : ' 접두사 제거. verifyCount(카운터=카드 수)는 행 수 기준 비교로 조정 — 유닛 분리 시 오경고 제거(actual=min(품목 수, 행 수)) ③ **e마트몰**: rowSelector를 :has(input[name=cbCartId])로 좁혀 추천상품·프로모션 카드 배제(V체크 상품만), 배송비는 주문하기 버튼 라벨 span.viewAmt_shppcst '(배송비 N원 포함)' 합산(3,000×2=6,000 — mhtml QP 디코딩 일부 글자 손상 대비 regex는 숫자만 매칭). html(최신)/mhtml 캡처 모두 검증 ④ 확장(1.5.5): 캡처 직전 사용자가 알아차리지 못할 정도(뷰포트 15%)로 살짝 스크롤 후 캡처 — 최상단 캡처의 문서 꼬리 잘림 방지(scrollTo(0,0) 제거). 검증: test-emart 14/14 + test-cart-fix 36/36 + test-feedback 23/23 + E2E 19샘플 err 0 | ✅ v1.13.0 |
| [🛠 5건 피드백: 네이버 축소 저장 순서·e마트몰 배송비·중복 삭제 버튼·문구·매핑 슬롯 ✕] ① 확장(1.5.5): 캡처 순서를 사용자 지정대로 재배열 — 25% 축소 적용 → **300ms 대기** → 스크롤·수집 → 500ms → 재수집 → 스티치·스탬프 → **축소 상태에서 outerHTML 저장** → **즉시 원래 배율 복원**(화면에 축소 과정 미노출) → 캡처 본문에서 html 태그 zoom 인라인 제거(뷰어 원래 크기) ② e마트몰 장바구니 배송비 누락 수정: 주문하기 버튼 라벨 `span.viewAmt_shppcst` '(배송비 N원 포함)' 배송유형별 표시값(3,000원×2 → 합산 6,000, 정답과 일치) ③ 중복 추출 삭제 버튼: 같은 쇼핑몰+품목명/규격/수량/단가 동일 행 그룹의 맨 오른쪽에 **[중복삭제]** 버튼 — 첫 1세트만 남기고 나머지 일괄 삭제(store.deleteDuplicateRows, 토스트 안내) ④ 시작 빈 화면 문구 변경: "품의캡처 북마크를 클릭하거나 품의 요구 확장 프로그램을 클릭하면 주문 화면이 여기에 표시됩니다" ⑤ 매핑 클릭 실수 복구: 행/필드 슬롯에 ✕ 버튼 — 잘못 고른 항목만 삭제 후 같은 단계에서 재클릭(removeRowSample·removeFieldSample), 배송비 미입력 저장은 기존 [건너뛰기]→mode:none으로 가능. 검증: test-emart 12/12 + test-cart-fix 33/33 + test-feedback 23/23 + E2E 19샘플 err 0 | ✅ v1.12.0 |
| [🛒 e마트몰(SSG.COM) 등록 + 추출결과 표 개편 + 네이버 25% 축소 캡처] ① 신규 규칙 2종: **emartmall-cart**(pay.ssg.com/cart/dmsShpp.ssg — 배송유형 섹션 흩어진 카드 div.mnodr_unit, 체크박스 input[name=cbCartId]의 checked 빈 속성이 MHTML에 보존되어 체크 행만 추출, 수량 input.ordQty value, 금액 em.ssg_price match:last=최종 판매가격[정상가 21,900→에누리→10,950], 라인합계 lineTotal 환산, 배송비는 카드 미표기로 주문서 담당) + **emartmall**(pay.ssg.com/order — 상품 행 tr.codr_unit_area[itemUnit_N], codr_unit_name[쿠폰 접두사 regex 제거], codr_unit_newprice 라인합계, codr_unit_count em.num=수량, 배송비 em.paySummaryOrdCstAmt). 하단 추천상품 모듈(ssgitem_*)은 자연 배제. 정답 xls는 시점 불일치(가격·담기 상태 상이)로 구조 추출 기준 검증. 장바구니·주문서·html 캡처 모두 규칙 매칭 확인 ② 추출 결과 표 개편: **첫 열에 쇼핑몰 이름 추가(문서별 셀 병합 rowSpan)**, 글자 크기 기본 **1.5배**(설정 ⚙에서 100~300% 사용자 지정, settings.json gridFontScale), **열 경계 드래그로 폭 조절**(px 기반 colgroup, 머리글 우측 핸들) ③ 확장(1.5.4): 캡처 전 화면을 **순간 25% 축소**(documentElement zoom)해 가상화 목록이 전체 행을 렌더하게 한 뒤 수집하고 **즉시 원래 배율로 복원** 후 outerHTML 캡처 — 네이버 등 창 가상화 몰의 행 누락 이중 방어(스티치+축소). 검증: test-emart 12/12 신설 + test-cart-fix 33/33 + test-feedback 23/23 + E2E 19샘플 err 0 | ✅ v1.11.0 |
| [🐞 4건 피드백 수정: 캡처 500ms 안정화·11st 복수 상품·배송비 금액별 유지·뷰어 이중 스크롤바] ① 확장(1.5.3) 캡처 직렬화 전 500ms×2 안정화 대기 추가(스크롤 완료 후 재수집 → 스티치·스탬프 → 500ms 후 outerHTML 전송) — 네이버/쿠팡 등 늦게 렌더링되는 행의 누락 방지 ② 11st 장바구니: 행 체크박스 name이 bcktSeq_B_0/B_1(그룹 내 순번)이라 고정 name(bcktSeq_B_0)에 그룹 내 2번째 상품부터 누락 → prefix(bcktSeq_) 매칭으로 수정 ③ 11st 주문서: rowSelector li.group_prd(판매자 그룹) → div.c_order_prd_row(상품 블록) — 그룹 내 상품 2개 이상 시 첫 상품만 추출되던 문제 수정(예제 캡처는 그룹당 1상품이라 미발견) ④ 엑셀 저장 배송비는 이미 금액별로만 묶임(aggregateShipping: 3000원×3+2500원×2 → 3000×3, 2500×2 두 행) — 신규 테스트로 확인, 전체 합산 없음 ⑤ 뷰어: iframe 내부 스크롤바 숨김 CSS 주입(html/body::-webkit-scrollbar 숨김 + scrollbar-width:none) — 축소 시 내부+외부 세로 스크롤바 2개 동시 노출 문제 해소(doc-size는 MutationObserver로 실시간 갱신 유지). 검증: test-cart-fix 33/33(11st 복수 상품 합성 2건·엑셀 배송비 금액별 신설) + test-feedback 23/23 + E2E 19샘플 err 0 | ✅ v1.10.3 |
| [🧩 복수 유닛 행 분리 + 네이버 가상화 방어 + 매핑 N슬롯] ① 사용자 교정(2차): G마켓 '플라스틱 정리함' 행처럼 한 상품에 옵션 라인이 여러 개면(멀티바스켓 1호·2호 각 5개) **각 라인이 독립 품목** — 엔진 신규 `units: { sel }` 스키마로 유닛별 수량·금액·옵션을 분리 추출(유닛 없는 행은 기존 행 레벨 단일 품목 유지). gmarket-cart에 units(dl.unit--item) + option(.option_value — '(-1,700원)' 가격 접미사 제거 regex로 규격 생성) 적용. 깃털 행도 2유닛(천연깃털 1@3,990 + 날개깃털 1@1,990) 분리 ② 네이버 V체크 누락(체크 6건→파일 4행, DTF 문자열이 HTML 어디에도 부재 실측 — 행이 스냅샷 DOM에 없었음)은 **목록 가상화**(화면 밖 행 미렌더) 탓 — 확장(1.5.0)이 캡처 전 페이지를 끝까지 스크롤해 모든 행을 렌더링 후 원위치 복귀한 뒤 outerHTML 캡처 + `[role=checkbox]`의 aria-checked도 data-arge-checked 스탬프화 ③ 새 쇼핑몰 규칙 만들기: 상품 개수 N 입력 시 **행·상품명·수량·주문금액·배송비 모두 N슬롯** 표시 — 각 항목을 상품별로 N번 클릭하면 자동 다음 단계, N샘플에서 공통 선택자 자동 도출(:nth-of-type/nth-child 제거 후 전부 일치하지 않으면 공통 꼬리 세그먼트 결합). 검증: test-cart-fix 30/30(복수 유닛 합성 2건 신설 — 멀티바스켓 1호/2호 각 5개@1,660 분리·폴백 확인, 신규 G마켓 3건 유닛 분리·부분저장 감지 4→3, 구 캡처 3건 유지, 네이버 회귀) + test-feedback 23/23 + E2E 19샘플 err 0 + 매핑 클릭 시뮬레이션 OK | ✅ v1.10.0 |
| [🛡️ 캡처 채널 행 누락 근본 수정(확장 1.5.0) + G마켓 할인가 match:last] 신규 G마켓 캡처 실측(2026-09-23 사용자 갱신본)에서 **확장의 saveAsMHTML도 체크 행을 드랍** 확인 — 계산기 '상품금액 93,300원=4품목'인데 멀티바스켓(2호) 행이 파일에 부재(수동 저장과 동일 Chrome 직렬화 결함). 확장(1.5.0) 캡처 방식 전환: ① 캡처 직전 checked **프로퍼티→표준 checked 속성** 동기화(사용자 제공 스니펫: set/remove checked) + data-arge-checked 스탬프 ② 살아있는 DOM outerHTML을 앱 /html로 전송(북마크릿과 동일 파이프라인 — mhtmlsave가 리소스 받아 MHTML 조립) → **행 누락 원천 차단**, executeScript 불가 시에만 saveAsMHTML 폴백. host_permissions 57331~35 추가. 북마크릿도 checked 속성 동기화 추가. **gmarket-cart 가격 match:last 신설** — 할인 행은 item_price strong 2개(원가 취소선 role=deletion + box__sale-amount 할인가), 깃털은 item_price 2개(쿠폰 안내 패널 3,990 + 우측 실제 금액 1,990)라 마지막 값이 실제 금액(정답 xlsx 바구니 5,520·멀티바스켓 1,660 일치, 구 정답 깃털 1,990과도 일치). 검증: new G마켓(스탬프 세트 볼링 3×78,000·깃털 1×1,990 추출 + 부분저장 감지 4→2 경고 정상) + test-cart-fix 28/28(할인행 단위 2건 신설) + test-cart-stamped 13몰 + test-cart-rules 13몰 + E2E 19샘플 err 0 | ✅ v1.9.0 |
| [🚨🚨 부분저장 감지(verifyCount)] 사용자 재현(장바구니_html/new, 2026-09-23): **Chrome 저장 스냅샷에 체크된 상품 행 일부 누락** — 네이버 화면 체크 6건 → 파일엔 4행만(DTF 품목은 파일에 아예 부재, PreSonus는 '내가 본 상품' 추천 모듈만 존재), G마켓 하단 계산기 '상품수 7개·상품금액 142,730·할인 -4,430' ↔ 파일 체크 스탬프 4행(누락 3행 = 물감 2×18,600+깃털 1,990, 금액 교차검증 일치). 앱이 파일에서 복구는 불가하므로 **불일치 감지·경고** 추가: 규칙 신규 스키마 `verifyCount: {sel, regex}` — 페이지가 알려주는 총수(네이버 'N개의 상품', G마켓 계산기 '상품수 N개')를 읽어 추출 수보다 크면 `countMismatch: {expected, actual}` 반환 → docstore 경유 → store.addDocs에서 경고 팝업("체크 N개 중 캡처에 M개만 저장…스크롤 후 재저장 또는 품의캡처 이용"). naver-cart·gmarket-cart에 셀렉터 등록(티처몰은 카운터 없어 미적용, 정답과 정확 일치). 검증: new 3파일(네이버 6→4·G마켓 7→4 감지, 티처몰 무경고) + test-cart-fix 24/24(부분저장 감지 체크 신설 포함, 네이버 새 캡처 기대값 3건→4건/6,000원 갱신·파일명 '장바구니-네이버' 하이픈 수정) + test-cart-rules 13몰 + E2E 전체 | ✅ v1.8.0 |
| [🎨 PenPDF 스타일 라이트 테마 전면 개편] 다크(slate-800 헤더·블루 액센트) → **모던 라이트 테마**: 앱 배경 #F4F6F8, 흰 카드(rounded-2xl + shadow 0 4px 12px rgba(0,0,0,0.03)), 프라이머리 보라 #5B4DFB(hover #4C3DE6), 틴트 #EEEDFE, 텍스트 #1E293B/#334155/#64748B, 보더 #E2E8F0. 헤더: 흰 배경+보라 타이틀, 버튼 3종(그레이 아웃라인/옐로우 틴트 #FEF3C7·#B45309/퍼플 틴트). 뷰어 탭 선택=#EEEDFE+보라 인디케이터. Grid: 헤더행 #F8FAFC·행 구분 #F1F5F9·셀 hover 보라 링, 빈 상태 3D 스타일 SVG 아이콘(뷰어/그리드 각각). 상태바: 정보 칩 rounded-full + '엑셀에 저장' CTA(보라 솔리드·hover scale 1.02). 토스트: 흰 카드+타입별 좌측 보더. 모달 전체: 오버레이 bg-slate-900/40+blur, 카드 rounded-2xl. 폰트 Pretendard→Noto Sans KR. styles.css에 :root 토큰(--primary 등) 정의. **로직 diff 0**(className만 수정, store/main/preload 미수정), rendererCheck.errs=[] E2E 확인 | ✅ v1.8.0 |
| [🐞 4건 버그 수정: 11st 수량·티처몰 장바구니·다이소 뷰 수량·아이스크림 V체크] ① **11번가 주문서**: `c_order_quantity .number` 수량 추출 + priceIs lineTotal(할인모음가=단가×수량 → 봉투 10개=1,060원 환산) ② **티처몰 장바구니** teachermall-cart 신규 — `tr:has(input.chk01)` 행, 수량 `input[id^=cart_cnt]` value, 단가 cost_info '개당 N원', 판매자 그룹 배송비 `em.fc_blue1`(checkedScope로 미체크 그룹 제외, 정답 6,000원 일치) ③ **다이소몰 장바구니 뷰어**: 규칙 `qtyInputSel` 신설 — 엔진이 계산한 수량을 mhtml 뷰의 수량 input value 속성에 주입(Vue 카트 수량 빈칸 해소, docstore가 주입 html 서빙) ④ **아이스크림몰**: 수동 저장 문서엔 V체크 상태가 파일에 존재하지 않음 확인(스탬프·checked·aria-checked·is-checked 전무) → 폴백 안내에 "품의캡처(익스텐션·북마크릿)로 다시 담으면 V체크만 자동 추출" 추가 + docstore `reflectCheckStates`(스탬프→checked 속성)로 캡처 채널 문서는 뷰어에도 V표시 ⑤ **G마켓 장바구니**: 페이지 개편 대응 — 그룹 배송비 셀렉터 `div.sub_sec.delivery strong.price`(하단 합계 계산기)→`div.shipping--info div.delivery strong.text__value`(그룹 footer, 4그룹 12,000원) ⑥ **네이버 장바구니**: 상점별 '무료' 대신 하단 예상금액 패널 '총 배송비 +3,000원' 채택(regex `\+` 필수 + first:true — 패널 2중 렌더·total_price_area 부모 중복 매칭 방지). **checkedScope 판정 하이브리드 재작성**: ①그룹 컨테이너형(G마켓, 배송비가 그룹 뒤)=다른 배송비 요소를 품지 않는 가장 가까운 조상의 박스로 판정 ②형제 행형(티처몰, 배송비 행이 그룹 앞)=다음 배송비 요소 전 구간의 박스로 판정. 검증: `analysis/test-cart-fix.js` 24/24 + test-feedback 23/23 + test-cart-rules 13몰 + E2E 2세트(errs 0, 티처몰 75MB 포함) | ✅ v1.7.3 |
| [📌📌 캡처 시점 V체크 박제(data-arge-checked)] v1.7.0의 checked 속성 필터는 **수동 MHTML 저장에서만** 동작 — 익스텐션(saveAsMHTML)/북마크릿(outerHTML)은 체크박스의 checked **프로퍼티를 직렬화하지 않아** G마켓 장바구니가 0건 추출→거부 팝업되던 것 수정. 익스텐션(1.4.3)·북마크릿이 캡처 직전 `input.checked` 프로퍼티를 `data-arge-checked="true/false"` **속성으로 박제**해 상태가 캡처에 함께 전송됨. 엔진 신규 `checkedOnly: {sel, legacySel}` — ① 스탬프 문서: true 행만 추출(스탬프 시뮬레이션 13몰 정답 정확 일치, 교보 AI페인팅·G마켓 한우도 정답대로 제외) ② 수동 저장: checked 속성/legacySel(다이소 label.is-checked) ③ 상태 전혀 없으면 전체 추출 폴백+안내 팝업(doc.checkedFallback → store.js `CART_FALLBACK_NOTICE`, 기존 몰별 고정 안내 대체). 12몰 카트 규칙에 checkedOnly 적용(예스24·옥션·드림디포·아이스크림·알파몰·오피스디포도 이제 캡처 채널에선 V체크만 추출). 카트 몰 0건 시 거부 메시지 "V체크된 상품이 없습니다…" 추가. bookmarks.js — 북마크릿 URL 변경 시 옛 URL 변종(`javascript:…127.0.0.1:573x…/html`)도 같은 🛒품의캡처 노드로 취급해 **기존 guid 보존·URL 갱신 교체**(중복 생성 방지) | ✅ v1.7.1 |
| [🛒🛒 장바구니 V체크 캡처 전면 지원] 주문서뿐 아니라 **장바구니 상태에서 품의캡처 → V체크한 상품만 자동 추가** (v1.7.0, 13몰 정답 대조 전수 통과 `analysis/test-cart-rules.js`). ① 체크 상태가 MHTML DOM에 보존되는 몰은 V체크만 추출: 교보(`input[checked]`)·알라딘·G마켓·11번가·쿠팡(`data-selected=true`∩`input[checked]` — 익스텐션/수동저장 두 경로 모두 정확)·네이버(`aria-checked=true`)·**다이소(`label.is-checked` Vue 클래스 — not_ok 폴더였지만 DOM엔 보존)** ② 체크가 소실되는 몰(드림디포·아이스크림몰·옥션·알파몰·예스24·오피스디포)은 전체 추출 + "선택하지 않은 상품은 삭제해 주세요" 안내 팝업(store.js `RULE_CART_NOTICES`) ③ 신규 몰: 알라딘·오피스디포·다이소몰 ④ 엔진 확장: `shipping.mode:'perItem'`(상품별 배송비 → "`<상품명> 배송비`" 행, 11번가), `shipping.first`(첫 값만, 알파몰), `qtyFromUnit`(합계÷단가 수량 계산, 다이소) ⑤ 내장 규칙 15종 → **25종**(`-cart` 접미사 규칙이 같은 도메인 주문서 규칙보다 우선) | ✅ v1.7.0 |
| 15종 내장 규칙 자동 추출 (G마켓·교보문고·네이버(주문+**장바구니**)·드림디포·아이스크림몰·알파몰·11번가·예스24·티처몰(주문+장바구니) + 옥션·쿠팡(장바구니)·엘레파츠·IC114·**롯데마트 제타**) | ✅ 총액 검증 완료 — IC114는 배송비 3,000원 고정(페이지에 배송비 없음), 주문서 캡처 거부 메시지 별도 |
| [🛒 네이버 장바구니 규칙] shopping.naver.com/cart 지원 — 해시클래스 prefix 매칭(`product--`/`title--` match:last로 배지 제외·'네이버플러스멤버십' 접두어 제거), em.price=합계(lineTotal→단가=합계÷수량), input number-- value=수량, 상점별 '총 배송비' 라벨 매칭(무료=0원). **rules.js builtin 순서: naver가 naver-cart보다 앞**(주문서 URL의 backUrl 쿼리에 shopping.naver.com/cart 원문이 포함되는 충돌 방지) | ✅ v1.6.8 |
| [ 🔤 charset 스마트 디코딩] mhtml.js `smartDecode` — 선언(헤더/meta)과 실제 바이트가 어긋나는 파일(BOM, euc-kr 라벨의 UTF-8, 64KB 이후 meta, UTF-16)을 후보별 디코딩 점수(한글×2−U+FFFD×50−mojibake×4)로 판별. MHTML 루트/HTML 파일/receiver /html 수신(메타 utf-8 재기록)/serveDoc 텍스트·CSS 파트 전 경로 적용 | ✅ v1.6.8 |
| [📌 북마크 중복 근본 수정] 프로필마다 다른 guid로 생성되면 Chrome 동기화가 서로 다른 북마크로 병합해 프로필마다 복제(P1·P3에 2개씩 실측) — ① add-bookmarklets가 **settings.json에 저장한 안정 guid(bookmarkGuid)를 모든 프로필에서 재사용**(생성 시·중복 정리 시 우선 보존) ② `/bookmark-info`·`/install`은 실제 바인드 포트와 무관히 **항상 57330 캐노니컬 북마크릿 URL** 반환(폴백 포트 URL을 내보내면 확장 self-heal이 포트별 변종을 계속 생성) ③ 확장(1.4.0) self-heal이 **전 폴더 url+title 중복 제거 + 예전 포트(57331~35) 변종 정리** | ✅ v1.6.8 |
| [📌📌 북마크 부활(v1.6.8 재발) 대응] 실측: 앱이 파일로 1개로 정리해도 옛 guid 복제본 부활(동기화 서버 재전송), 확장은 전 프로필 미설치(런타임 힐 무력). 원인: (a) 백그라운드 프로세스(시작 부스트)가 옛 북마크 모델을 다시 플러시 (b) 파일 삭제는 동기화 서버에 tombstone으로 반영되지 않음. 대응: ① `Bookmarks.bak` 동일 내용 기록 ② closeBrowserGracefully 강제종료 후 프로세스 완전 소멸 확인 + read-back 검증·재시도(isCaptureBookmarkAtFront) ③ 사전/사후(30초 뒤) 중복 감지 → 북마크 관리자 열기 다이얼로그 ④ 재시작 `--load-extension` | ✅ v1.6.9 |
| [📌📌📌 북마크 "삭제 직후 버튼" 함정 방지(v1.6.9 재발-2 대응)] | 사용자가 북마크 관리자에서 중복을 삭제한 직후 바로 버튼을 누르면, 앱이 크롬을 강제종료하며 **tombstone(삭제 기록)이 동기화 서버에 업로드되기 전에 크롬이 죽어** 옛 복제본이 재전송됨(교사박동배=Default 프로필에서 19개 부활 실측: 옛 guid 18 + 신규 1). 대응: ① 버튼 시작 시 **직전 실행 때 북마크가 있던(settings.bookmarkCounts) 프로필이 지금 0개 + 브라우저 실행 중**이면 "2~3분 더 열어둔 뒤 직접 닫고 다시 눌러주세요" 경고(취소/계속 선택) ② 쓰기 후 현재 개수를 settings.bookmarkCounts로 저장 | ✅ v1.6.10 |
| [🚨🚨 확장 무한 생성 루프 사고 수정(v1.6.10 재발-3 대응)] | v1.6.9가 브라우저 재시작에 붙인 `--load-extension`을 Chrome 153이 **실제로 로드** → 확장 self-heal이 북마크릿 URL의 Chromium 저장 시 **URL 정규화 차이**로 자기가 만든 북마크를 검색하지 못해 **무한 생성 루프**(교사박동배 프로필 361개, 확장 삭제로 정지 확인). 수정: ① 재시작 플래그 **완전 제거** ② 확장(1.4.1) heal **생성 금지·이동/중복제거 전용** 재작성 ③ heal **회로차단기**(8회/60초) | ✅ v1.6.11 |
| [🛒 롯데마트 제타 규칙] lottemartzetta.com/basket 지원 — `data-test` 속성 기반(fop-product-link=상품명, summary-cost-delivery-charge=배송비, input._counter__input value=수량), 가격은 `_display_` 클래스 첫 매칭(합계, lineTotal→단가=합계÷수량) — 정답 대조 완료(쉐푸드 치킨 3@8,320 · 미네랄워터 1@2,000 · 배송비 3,000, test_OK/롯데마트) | ✅ v1.6.12 |
| [🎨 캡처 스타일 누락 수정] Chrome `saveAsMHTML`/outerHTML은 **CSSOM 주입 스타일(adoptedStyleSheets·shadow DOM)을 캡처에서 누락** → 롯데마트 등에서 요소가 세로로 나열됨(실측: 실시간 캡처엔 인라인 스타일시트 파트 0개 vs 수동 저장엔 58KB). 확장(1.4.2, scripting 권한 추가)은 캡처 직전 `document.adoptedStyleSheets`(+shadowRoot 1단계)를 DOM `<style data-arge-adopted>`로 박제 후 saveAsMHTML, 북마크릿도 동일 주입 — 앱의 URL 재작성 파이프라인은 검증 결과 무결(CSS 3종 모두 app-mhtml:// 로컬화 확인) | ✅ v1.6.12 |
| [🧭 매핑 UX 개선] ① 캡처 대기 화면(좌측 뷰어+매핑 배너)에 "2개 이상 상품" 안내 ② 매핑 패널에 **상품 개수 입력** 추가 — 행 클릭 시 `count-selector` IPC(cheerio)로 선택자 매칭 행 수를 계산해 일치/불일치 배지 표시 | ✅ v1.6.12 |
| [🐞 잠복 버그: 문서 ID 대문자 → app-mhtml 무작위 404] docstore가 UUID 앞 8자를 id로 쓰는데 **URL hostname은 대문자를 소문자로 강제 변환** — 대문자 포함 id가 나오면 뷰어 iframe·프로토콜 조회가 `doc not found`로 실패. id를 **항상 소문자 생성**으로 수정. E2E에 cssPartCheck(CSS 파트 응답 검증)·renderCheck(프레임 내 스타일시트 적용 수/li 가로배치/폰트 실측)·E2E_SHOT(스크린샷) 추가, E2E_DIR을 shoping_cart보다 우선 적용하도록 수정 | ✅ v1.6.13 |
| [🧭 매핑 행 다중 선택·일반화] "한 품목만으로 규칙 생성 시 2개 이상에서 항목 누락" 수정 — picker cssPath가 남기는 `:nth-of-type(n)`이 다른 행 매칭을 막았음. ① 행 클릭 시 **서수 제거한 선택자로 일반화** ② 행 단계가 자동 진행되지 않고 **여러 행을 계속 클릭** 가능(공통 선택자 자동 계산, [다음: 상품명 지정] 버튼으로 진행) ③ 안내 문구 "2개 이상의 상품을 선택 하세요."로 통일(대기 화면·배너·시작 토스트) | ✅ v1.6.13 |
| [🧭 행/열 구분 선택 매핑] 상품이 **행(세로 나열)**이 아닌 **열(표에서 가로 나열)**로 구분되는 쇼핑몰 지원 — ① 매핑 ①단계에 **상품 구분 [행/열] 라디오** ② 열 모드: 첫 상품 셀 클릭으로 표+첫 상품 열 지정 → 상품명/수량/가격/배송비 클릭이 각각 표의 **줄 번호**로 기록 ③ 규칙 스키마 `orientation:'column'`(tableSelector·firstProductCol·nameRow/qtyRow/priceRow/shippingRow) ④ extract.js `extractItemsByColumn`(표 열 순회, 단가=합계÷수량 동일) ⑤ picker가 클릭에 표/셀 좌표(rowIndex·cellIndex) 동봉 ⑥ count-selector IPC 열 모드 상품 수 계산 — 합성 열-표 테스트(3품목·lineTotal·배송비) 통과 | ✅ v1.6.14 |
| 캡처 채널: 익스텐션(MV3, Chrome/Edge/웨일 호환) MHTML / 북마크릿 🛒품의캡처 HTML+소스URL | ✅ |
| [⭐ 북마크바 추가] 브라우저 북마크바 제일 앞에 자동 삽입(실행 중이면 확인→종료→삽입→재시작, 프로필별 백업/중복제거) | ✅ v1.5.3 3중 안전장치: 체크섬 삭제 + 쓰기 후 2초 간격 3회 검증·재적용(시작부스트 복귀 방어) + 확장 self-heal(런타임 API로 index 0 생성/이동, onStartup/onInstalled) |
| [🧩 익스텐션 추가] 모달: 확장페이지 링크 / 개발자모드 강조 / 브라우저별 자동 로드(--load-extension) / 경로 복사 | ✅ v1.5.3 "확장 프로그램 로드"는 브라우저를 실행하지 않고 경로 클립보드 복사+하단 안내만 |
| [💰 배송비 합산 저장] 최종 xls 저장 시 배송비를 가격별로 합산 (3000원x1+3000원x2+4000원x1 → 3000원x3 + 4000원x1) | ✅ v1.5.0 excel.js appendRows에서 처리(누적/다른경로 저장 모두 적용) |
| [🚫 주문서 아닌 캡처 거부] 캡처에서 품목 0건 추출 시 문서·inbox 파일 삭제 후 팝업(매핑 대기 중 제외) — 몰별 메시지: 기본 "주문서 상태에서~" / 네이버 "V버튼을 눌러~" / 쿠팡 "장바구니 상태에서~" / IC114 "장바구니 상태에서 품의캡쳐를 누르세요. 주문하기 화면에서 배송비는 반드시 확인해 보세요." | ✅ v1.6.1 |
| [💾 저장 교체 의미론] "엑셀에 저장"은 선택한 파일을 **현재 표 내용으로 교체**(.bak 백업 후 재생성) — 0% 저장 후 10% 재저장 시 중복 행 발생 버그 수정, 누적은 "불러오기→추가→저장" 플로우로 | ✅ v1.6.1 |
| [↔️ 분할바 개선] 드래그 중 iframe 이벤트 차단 오버레이 + 설정 저장은 드롭 시 1회 — 끊김 없는 좌우 드래그 | ✅ v1.6.1 |
| [🔍 뷰어 줌/스크롤 개편] 단일 스크롤 컨테이너(패널 `overflow:auto`) + sizer(측정 문서크기×zoom) + iframe 자연 크기 고정 후 `scale(zoom)` — 축소 시 이중 세로 스크롤바·빈 공간·문서 하단 가로바 제거, 배율 변경 시 중심점 유지 | ✅ v1.6.2 |
| [📌 북마크 런타임 self-heal] 확장이 `onMoved`/`onChildrenReordered`/`onCreated`(800ms 디바운스)로 맨앞 재고정 + 동일 url+title 중복 정리 + 시작 5초 후 재확인 — Chrome 시작 직후 동기화 등이 북마크를 맨뒤로 밀어내는 현상 방어 | ✅ v1.6.2 |
| [📌 북마크 동기화 근본 수정] `addBookmarkToFront`가 기존 노드를 **삭제 후 새 guid로 재생성하지 않고 기존 guid/id/date_added 노드를 그대로 맨 앞으로 이동** — 새 guid로 재생성하면 Chrome 동기화가 서버의 옛 guid 노드를 원래 위치(맨뒤)로 부활시켜 밀려나던 것. 북마크바·other·synced 전체에서 기존 노드 탐색→있으면 **생성 금지·이동만**, 없을 때만 생성. 확장 runtime heal도 `bookmarks.search` 전체 검색 후 기존 노드 이동(없을 때만 create) | ✅ v1.6.3 |
| [✋ 뷰어 pan] 왼쪽 미리보기에서 **우클릭 또는 휠버튼 드래그로 화면 잡아끌기** — iframe 내부는 주입 스크립트가 movement 델타를 postMessage로 전달, iframe 밖(배경/엑셀뷰)은 컨테이너 mousedown, pan 중 오버레이로 마우스 이벤트 통합·커서 grabbing 표시, pan 중 우클릭 메뉴/휠클릭 기본동작 차단 | ✅ v1.6.3 |
| [🔔 북마크 추가 완료 팝업] 북마크바 추가 후 안내 팝업 — "크롬 브라우져인 경우 북마크바 동기화 상태일 때는 북마크바가 제일 뒷쪽에 생성 됩니다. 클릭 후 편리한 곳으로 드래그 해주세요." (브라우저 재시작 후 표시) | ✅ v1.6.4 |
| [🌐 확장 페이지 열기 수정] browser-action이 **URL 단독 첫 인수 + `--load-extension`** 로 실행 — 기존 `--profile-directory` 플래그 동반 시 실행 중 인스턴스 릴레이에서 URL이 무시되어 빈 창만 열리던 것 수정(Chrome/Edge/웨일 공통). 브라우저 미실행 시 확장 페이지에 '품의 생성기' 자동 등록까지 시도 | ✅ v1.6.4 |
| [🧩 익스텐션 모달 초보자 개편] ①브라우저 큰 버튼 → ②'품의 생성기' 카드 확인(성공 안내) → ③안 보이면 3-1/3-2/3-3 세분계(개발자모드→로드→경로붙여넣기) 구조, 글자 크기·단계 강조 개선 | ✅ v1.6.4 |
| [🔍 휠 줌] 왼쪽 미리보기에서 **마우스 휠로 확대/축소**(노치당 10%, 25~300%) — 커서 위치를 줌 기준점으로 유지, iframe 내부는 주입 스크립트가 휠 이벤트 전달(preventDefault로 스크롤 체이닝 차단), 트랙패드 대비 delta 누적 처리. 문서 뷰에서만 동작(엑셀뷰/대기화면 제외), 스크롤은 pan/스크롤바로 | ✅ v1.6.4 |
| [🔍 휠 줌 감도 절반] 노치당 10% → **5%**로 감소 | ✅ v1.6.5 |
| [🧩 익스텐션 모달 → 수동 설치서] 자동 시도 대신 **완전 수동 설치 안내**로 재구성(브라우저 탭 Chrome/Edge/웨일 × 6단계, 0. 공통 준비 폴더 확인, 설치 후 공통 확인) — 확장 페이지 주소·폴더 경로는 **버튼 클릭 시 클립보드 자동 복사** | ✅ v1.6.5 |
| [⚙ 설정 패널] 헤더 우측 ⚙ 설정 — ① "새 쇼핑몰 규칙 추가" 버튼 표시 토글(**기본 끄기**, settings.json showRuleAdd) ② **사용 매뉴얼(PDF) 보기** — 매뉴얼은 `app/resources/manual.pdf`(pdfkit+맑은고딕, `analysis/make-manual-pdf.js`로 생성, extraResources로 패키징 후 userData에 복사해 열기) | ✅ v1.6.5, v1.6.6 확장 |
| [🗑 시작화면 등록카드 제거] v1.6.5의 시작화면 북마크 등록 카드는 사용자 혼란으로 **완전 제거** — 관련 IPC(bookmark-status/remove-bookmarklets)·store 액션·bookmarks.js removeBookmarklet도 함께 정리 | ✅ v1.6.6 |
| [🖱 휠 줌 감도 설정] 설정 패널에 슬라이더(0.5~10%, 기본값 버튼) — **기본 1.7%/노치**(v1.6.5 감도 5%의 1/3), settings.json zoomSensitivity 저장, zoom 정밀도 0.001로 확대 | ✅ v1.6.6 |
| [🚀 시작 프로그램 등록/삭제] 설정 패널 — HKCU Run 레지스트리(AutoRequisitionGenerator 값), 등록 상태 배지+대상 경로 표시, [등록] 시 **이미 등록되면 "등록 되어 있습니다." 알림**, portable exe는 PORTABLE_EXECUTABLE_DIR의 원본 exe 경로 등록(TEMP 경로 방지), IPC startup-status/register/remove | ✅ v1.6.6 |
| [🔍 텍스트 노드 공백 정규화] extract.js `extractField`: text 노드는 내부 개행/탭을 단일 공백으로 정규화(오피스디포 주문서 상품명 포함 2개 항목 추출). 공백 정규화는 이름·배송비 금액 패턴 매칭에 유리. 기존에는 `.text()` 사용으로 내부 공백 유지 → 오피스디포 상품명 정규식 `^\(\d+\)\s*(.*)$` 실패. 공백 정규화 후 `^\(\d+\)\s*([\s\S]*)$` 성공 → 모든 상품 추출 정상 작동. | ✅ v1.7.2 |
| [🛒 드림디포 주문서 규칙] dreamdepot-order 신규 (드림디포 공식쇼핑몰 /or/ora/SCR_ORA_0001). ① rowSelector: `tr:has(td.qt)` (수량 체크 → 2행). ② name: `td.info`에서 `^\\(\\d+\\)\\s*(.*)$` 정규식 + 공백 정규화. ③ price: `div.goods-price` (페이지에서 합계표시, 단가 아님). ④ qty: `div.goods-num`에서 "N개". ⑤ 배송비: 결제창 `div.val` (중첩된 "기본 배송비" 제외, 첫 직계 자손). ⑥ checkedFallback: true (주문서는 체크 상태 유지 → 수동 저장에서도 V체크됨). 정답: 상품 2건 49,200원 + 배송비 3,000원. | ✅ v1.7.2 |
| [🛒 다이소몰 주문서 규칙] daisomall-order 신규 (다이소몰 주문 페이지 /or/ora/SCR_ORA_0001). ① rowSelector: `div.goods-unit.order` (2행). ② name: `div.tit` (빈 a태그 앞에 위치). ③ price: **lineTotal** (화면에서 합계만 표시 → 단가=합계÷수량: 3,000원→1,000원, 10,000원→5,000원). ④ qty: `div.goods-num`에서 "N개". ⑤ 배송비: 결제창 `div.val` (중첩된 "기본 배송비" 제외, 첫 직계 자손). ⑥ checkedFallback: true. 정답: 상품 2건 (3×1,000원 + 2×5,000원) + 배송비 3,000원. | ✅ v1.7.2 |
| [🛒 알라딘 주문서 규칙] aladin-order 신규 (알라딘 배송정보 입력 페이지). ① rowSelector: `tr.product-row` (2행). ② name: `td.prod-name`에서 `^[^0-9](.*)` (앞의 가격 제거). ③ price: `td.prod-price`에서 `^\\d{1,3}(?:[,\s]\\d{3})*원` (존재 여부 확인). ④ 배송비: 0 (가격=0이면 배송비 없음). ⑤ checkedFallback: true. 정답: 상품 2건, 배송비 0원. | ✅ v1.7.2 |
| [🛒 오피스디포 주문서 규칙] officedepot-order 신규 (오피스디포 공식쇼핑몰 주문 페이지). ① rowSelector: `tr:has(td.qt)` (2행). ② name: `td.info`에서 `^\\(\\d+\\)\\s*(.*)$` + 공백 정규화. ③ price: `td.bm`에서 `^\\d{1,3}(?:[,\s]\\d{3})*원` (선불 결제 금액). ④ qty: `td.qt`. ⑤ 배송비: 0 (선불 결제이므로 배송비 별도 표시 없음). ⑥ checkedFallback: true. 정답: 상품 2건 157,480원 + 배송비 0원. | ✅ v1.7.2 |
| [📊 E2E checkedScriptOk 검증] E2E 결과에 `checkedScriptOk` 추가 — 뷰어 HTML에 data-arge-checked/data-selected 스크립트 존재 여부 확인. v1.6.12의 checkedState 동기화가 잘 적용되었는지 정량 검증. 18개 캡처 파일 전수에서 checkedScriptOk=true, rendererCheck.errs=[], mappingCheck={started:true,waiting:true,frame:"ok",rowClick:true,nameClick:true}. E2E_RESULT JSON에 `checkedScriptOk: true` 포함. | ✅ v1.7.2 |
| [🧹 매핑 화면 렌더링 개편] MHTML 리소스 로컬 해석 정밀화 — ① rewriteUrls가 **`&amp;` 이스케이프 URL도 매칭**(파트 헤더는 raw `&`라 기존엔 쿼리 붙은 CSS/이미지 전부 미매칭→깨진 렌더링이었음) ② **CSS 파트 내부 `url()`/`@import` 재작성**(파트 매칭→app-mhtml://, 미매칭 상대경로→원본 절대 URL, data: 보존) ③ **매핑 모드 iframe 최소 폭 1280px** 보장(반응형 몰의 모바일 레이아웃 붕괴 방지, 일반 뷰어는 기존 자연 폭 유지) | ✅ v1.6.7 |
| [👁 자동 포커스] 파일 열기/품의캡쳐 수신 시 해당 문서를 자동 선택해 좌측 뷰어에 즉시 표시 | ✅ v1.5.1 |
| 쿠팡 장바구니 V체크된 상품만 추출(div[data-selected=true] — 미체크 제외, test_OK/new 정답 xls와 251,770원 일치 검증) | ✅ v1.5.1 |
| [📸 북마크릿 HTML→MHTML 변환] /html 캡처 수신 시 CSS/이미지 리소스를 net.fetch로 받아 MHTML로 저장해 좌측 뷰어가 정돈되게 표시(실패 시 .html 폴백) — 쿠팡 등 Tailwind 페이지 필수 | ✅ v1.5.2 |
| [📊 엑셀 불러오기 개편] 불러오기 시 좌측 엑셀 그리드 뷰(행/열 번호·병합·시트 드롭다운·확대) + 우측 테이블 자동 반영(교체/뒤에 추가/취소 다이얼로그, source:'excel') — excel.MD v2 구현 | ✅ v1.6.0 |
| [💾 저장 단일화] "엑셀에 누적 저장하기" 삭제, "📂 다른 경로에 저장"→**"엑셀에 저장"**(primary) 승격 | ✅ v1.6.0 |
| [↔️ 분할바] 좌/우 패널 드래그 조절(기본 좌 2/3·우 1/3, 더블클릭 복원, 비율 settings 저장) | ✅ v1.6.0 |
| [％ 단가 인상] 상태바 우측 단가 인상 % 선택(기본 0%) — 엑셀에 저장 시 예상단가에 반영, **배송비는 제외** | ✅ v1.6.0 |
| [✏️ 규칙 이름 수정] 내장 규칙 포함 전체 이름 수정 가능(overrides.json) | ✅ v1.5.0 |
| [📊 북마크 진행률] 북마크바 추가 작업 중 브라우저/프로필 진행 상황 오버레이(Book.MD 반영) | ✅ v1.5.0 |
| [새 쇼핑몰 규칙 추가] 캡처 대기 방식: 앱 대기 → 사용자 본인 브라우저에서 캡처 → 파일로 저장·표시 → 6단계 클릭 매핑 | ✅ v1.3.0 |
| 매핑 4단계 "주문금액" → 예상단가 = 주문금액÷수량 자동 환산 | ✅ v1.4.0 |
| 규격 자동 생성(품목명/옵션 정규식, test_OK 정답 패턴, 정확도 21/22) | ✅ |
| 그리드 셀 클릭 편집 / ＋행 추가(수동 문서 폴백) / 행 삭제 | ✅ |
| [🗑 규칙 관리] 사용자 규칙 삭제 | ✅ |
| 엑셀 누적 저장 + [📂 다른 경로에 저장] (.bak 백업, BIFF8) | ✅ |
| 뷰어: iframe 줌(25~300%), 단일 스크롤 컨테이너 방식(v1.6.2 — 문서 전체 크기 iframe + sizer×zoom), `<base href>`로 캡처 이미지 웹 로드 | ✅ |
| 단일 인스턴스 잠금, 포트 폴백 57330~57335, 앱 Ctrl+휠 줌 고정 | ✅ |

## 3. 핵심 아키텍처 (app/)

```
src/main/index.js        창/IPC/프로토콜(app-mhtml://)/수신서버/E2E(runE2E, env E2E=1)
src/main/lib/
  mhtml.js               MIME 파서(QP/base64, charset 스니핑)
  extract.js             규칙 엔진 (아래 §4 스키마)
  spec.js                deriveSpec 규격 자동 생성
  rules.js + rules/*.json  내장 32종 규칙 (사본: analysis/rules/) — builtin 배열 순서=매칭 우선순위(-cart 규칙이 같은 도메인 주문서 규칙보다 앞 / naver가 naver-cart보다 앞: backUrl 쿼리 충돌 방지 / teachermall-cart가 teachermall보다 앞, overrides.json으로 이름 오버라이드)
  docstore.js            문서 로드(MHTML/HTML 자동 판별, fixLazyImages, base href), reextract
  excel.js               SheetJS .xls 읽기/추가/생성 (bookType biff8) + loadExcelFull(헤더 별칭 탐지·품목 행 파싱, excel.MD §3)
  receiver.js            HTTP 수신 /mhtml /html(GET /ping /install) — /html은 mhtmlsave.js로 HTML→MHTML 변환 후 저장(리소스 포함, 뷰어 정돈)
  mhtmlsave.js           북마크릿 HTML → MHTML 변환(cheerio로 CSS/이미지 URL 수집·절대경로화 → net.fetch 다운로드 → multipart/related 조립)
  bookmarks.js           북마크바 JSON 편집(제일 앞 삽입, .arge-bak 백업, 중복 제거)
  bookmarklet.js         북마크릿 URL 빌더 (57330 고정, receiver와 공유)
  picker.js              PICKER_SCRIPT(iframe 주입용; GUEST_PRELOAD_SOURCE는 미사용 레거시)
src/preload/index.js     contextBridge API
src/renderer/            React+Zustand+Tailwind v4 (Header/Viewer/Grid/StatusBar/MappingModal/ExtensionModal/RulesModal/Toasts)
extension/               Chrome/Edge/웨일 MV3 확장 v1.5.0 (패키징 시 extraResources로 복사) — 캡처는 live DOM outerHTML + checked 속성 동기화 + 캡처 전 페이지 스크롤 렌더(목록 가상화 방어) → /html 전송(1순위), saveAsMHTML은 폴백. bookmarks 권한으로 북마크바 맨 앞 self-heal(onStartup/onInstalled, /bookmark-info에서 이름·URL 동기 수신)
analysis/                규칙 도구: decode_all.js, peek.js, probe-rows.js, test-rule.js, test-spec.js, test-bookmarks.js, test-bookmarklet.js, test-browseraction.js, make-rules-json.js(깃허브 업데이트용 rules.json 생성) + decoded/(디코드된 샘플 HTML)
```

**IPC 목록:** open-mhtml-files/folder, load-mhtml-path, remove-doc, get-doc-url, reextract-doc, update-rows, list-rules, save-user-rule, delete-user-rule, rename-rule, reject-doc, alert-box, read-excel, pick-excel, load-excel-full, excel-load-confirm, append-excel(호환 유지), save-excel-as, get/set-setting, open-extension-folder, extension-info, copy-text, browser-action(key, action: open-extensions|load-extension), add-bookmarklets, startup-status/register/remove, open-manual, reveal-file + 이벤트 mhtml-received, bookmark-progress

**사용자 데이터:** `%APPDATA%\자동 품의 요구 생성기\` — settings.json(excelPath, lastSourceUrl), rules/(사용자 규칙), inbox/(캡처 저장+sidecar .url.txt), extension/(패키징시 고정 복사 — portable 임시경로 문제 해결용)

## 4. 규칙 엔진 스키마 (중요)

```json
{
  "id": "gmarket", "name": "G마켓", "match": ["gmarket.co.kr"],
  "rowSelector": "div.box__goods-info.product__list",
  "priceIs": "lineTotal",            // 화면금액=단가×수량 합계 → 단가=금액÷수량 환산 (gmarket/naver/teachermall)
  "fields": {
    "name":  { "sel": "...", "attr": "text|value|...", "regex": "...", "group": 1, "match": "last" },
    "qty":   { ... }, "price": { "match": "last" },   // match:last = 여러 strong 중 마지막(G마켓 쿠폰할인가)
    "option": { "sel": ".box__option" }               // 규격 생성에 사용 (gmarket/naver)
  },
  "shipping": { "mode": "selector|row|conditional|perItem|none", "sel": "...", "regex": "...", "first": true, "fee": 3000, "freeOver": 50000, "rowMatch": "배송비" },
  "qtyFromUnit": "셀렉터"  // 수량 속성이 없는 몰: 수량=합계÷단가 (다이소)
}
```

- 사용자 매핑 규칙은 `priceIs: 'lineTotal'` 고정(v1.4.0 요청)
- test_OK/품목내역(통합).xls = 정답 파일: 파워에이드 23,970 = 239,660(쿠폰가)÷10 올림 등으로 검증

## 5. 빌드/배포/테스트 (환경 특이사항 포함)

```powershell
cd app
npm install            # 최초 1회. 이후 npm approve-scripts electron esbuild 필요(allowScripts 정책)
npm run build          # electron-vite 번들 (out/)
# E2E (dev): 개발 E2E는 싱글인스턴스 잠금/포트 충돌과 무관하게 userData 격리됨
$env:E2E='1'; $env:E2E_DIR="..\shoping_cart"; $env:E2E_OUT="<json경로>"; npx electron .
# 패키징 (winCodeSign 심볼릭링크 이슈로 아래 플래그 필수)
$env:CSC_IDENTITY_AUTO_DISCOVERY='false'
npx electron-builder --win portable --config.win.signAndEditExecutable=false
# 배포 교체 (사용자 앱 실행 중이면 파일 잠김 → question 도구로 닫아달라고 요청)
Copy-Item release\AutoRequisitionGenerator-Portable.exe "..\자동품의요구생성기_Portable.exe" -Force
# 패키징 exe E2E: exe 직접 실행 + 같은 env. **E2E_DIR/E2E_OUT은 절대경로 필수** (portable exe는 %TEMP%에서 실행되어 상대경로가 틀어짐)
```

**E2E 검증 항목(runE2E):** 10개 샘플 전수 파싱(shoping_cart; G마켓 2개=8+6건) · app-mhtml 프로토콜/피커 서빙 · receiver(/ping,/install,/html 캡처→G마켓 인식) · 엑셀 생성/추가/재독 · 렌더러 콘솔 에러 0 · **매핑 클릭 시뮬레이션**(webFrameMain.frames → iframe 내 요소 .click() → rowSelector/picks.name 검증) — 결과 JSON의 `mappingCheck`
**장바구니 규칙 정답 대조:** `node analysis/test-cart-rules.js` — `장바구니_html/` v_check_ok·v_check_not_ok 13몰 캡처 × 정답 xls 전수 비교(수량·올림단가·배송비). 교보 'AI 인공지능 페인팅'·G마켓 '농협안심한우'는 캡처 시점 체크된 정답 외 항목으로 KNOWN_EXTRAS 등록
**스탬핑 캡처 시뮬레이션:** `node analysis/test-cart-stamped.js` — 정답 행의 체크박스만 data-arge-checked=true로 박제한 HTML(=익스텐션/북마크릿 실제 캡처 형태)로 추출해 13몰 정답과 정확 일치 검증(폴백 없음 확인 포함)

**환경 주의:**
- PowerShell 5.1 콘솔 한글 깨짐 → 파일 조작/한글 포함 로직은 node 스크립트로 (analysis/ 참조)
- omo 서브에이전트: 2026-09-23 `~/.config/opencode/oh-my-openagent.jsonc`를 **원래 zai API로 복원**(beat 프록시 127.0.0.1:8787 다운 → task() 위임 전부 "Cannot connect to API" 실패). 무거운 에이전트=`zai-coding-plan/glm-5.2`·explore/librarian/quick=`glm-5-turbo`·fallback=`glm-5.3-flash` (2026-09-16 검증 조합). 이전 beat 프라이머리 구성 백업: `oh-my-openagent.jsonc.backup-beat-2026-09-23`. **설정 변경 후 opencode 재시작 필요**
- omo 서브에이전트: 2026-09-16 `~/.config/opencode/oh-my-openagent.json`의 모델 ID를 전부 `zai-coding-plan/glm-5.2`(무거운 에이전트)/`glm-5-turbo`(explore·librarian·quick)로 교체해 수정 — 이전엔 opencode 프로바이저 API 키 무효 + glm-4.6v/glm-5 미존재로 "Sisyphus-Junior not found"·Sisyphus 스트림 오류 발생. 백업: `oh-my-openagent.json.backup-2026-09-16T20-39-03`. **설정 변경 후 opencode 재시작 필요**
- 사용자 앱이 자주 실행 중 → 배포 교체 전 프로세스 확인(`*품의*`,`*Portable*`), question 도구로 종료 요청
- E2E 후 좀비 프로세스 정리 습관화 (이전에 EADDRINUSE 사고 발생)

## 6. 알려진 제약/유의점

- 아이스크림몰 lazy-load 상품명은 MHTML에 없음 → "로딩 중" 추출, 수동 수정
- G마켓 기존 주문서: 추출 합계 567,160 vs 페이지 상품금액 575,110 (7,950원 = 정적 HTML 미표기 옵션금 추정)
- 카프리썬류 "골라담기" 맛 조합은 정적 파싱 불가 → 40개입, 200ml 등으로 대체
- 최신 Chrome stable(v137+)은 --load-extension 차단 → 북마크릿 안내 (Edge/웨일은 바로가기/자동 로드 동작)
- 북마크 편집은 브라우저 **완전 종료 상태**에서만 유효 (실행 중 종료 요청→삽입→재시작 자동화로 처리)
- **동기화 로그인 프로필의 북마크는 파일 삭제로 지워지지 않는다** — 파일 삭제는 동기화 서버에 tombstone으로 반영되지 않아 옛 엔티티가 계속 부활함. 중복 수렴은 반드시 브라우저(북마크 관리자 또는 확장 런타임) 삭제로 1회 처리해야 계정 전체에 전파됨 (v1.6.9 교훈)
- iframe 피커는 앱이 `picker-mode` 메시지를 iframe에 postMessage로 전송해야 단계 진행(v1.0.x의 "클릭 안 됨" 원인 — 재발 주의)
- `findBrowserByKey` 등 **async 호출부 await 누락 주의** (v1.4.1 버그의 원인, Promise가 객체처럼 쓰임)

## 7. 버전 이력 요약

v1.0.0 초기(9몰 규칙·portable exe) → v1.0.1 포트폴백/단일인스턴스 → v1.0.2 G마켓 다중상품 버그+lineTotal 환산 → v1.0.3 규격 자동생성+셀 클릭 편집 → v1.0.4 경로지정 저장·뷰어 스크롤·매핑 모달 우측 배치·G마켓 할인가(match:last) → v1.1.0 브라우저 원클릭 연동/북마크릿 /html → v1.2.0 라이브 브라우저 매핑 → v1.2.1 base href 이미지 수정·웹뷰 표시·클립보드 → v1.2.2 웹뷰 줌/크기 방어 → v1.3.0 캡처 대기 매핑+picker-mode 버그 수정 → v1.4.0 주문금액·규칙삭제·행추가·익스텐션 추가 모달·북마크바 추가 → v1.4.1 await 버그 수정 → v1.5.0 신규 4몰·배송비 합산 저장·캡처 거부 팝업·규칙 이름 수정·북마크 진행률 → v1.5.1 확장페이지 프로필 인식·뷰어 자동 표시·몰별 거부 메시지·쿠팡 V체크 추출 → v1.5.2 북마크 체섬검사·티처몰장바구니 규칙 통합·HTML→MHTML 변환·익스텐션 모달 안내 → v1.5.3 북마크 3중 안전장치·확장 로드 브라우저 미실행 → v1.6.0 엑셀 불러오기 개편·"엑셀에 저장" 승격·좌우 분할바(2/3)·단가 % 인상 저장 → v1.6.1 IC114 규칙(배송비 3,000 고정+전용 거부 메시지)·저장 교체 의미론(0%/10% 중복 해결)·분할바 드래그 매끄럽게 → v1.6.2 뷰어 줌/스크롤 개편(단일 스크롤 컨테이너, 이중 스크롤바·빈 공간 제거)·북마크 런타임 self-heal(이벤트 리스너 재고정)·앱 시작 시 %APPDATA% 확장 자동 갱신(extensionFolder 호출)·확장 manifest 1.2.0 → v1.6.3 북마크 동기화 근본 수정(기존 노드 guid 보존 이동 — 재생성 시 동기화가 옛 guid를 맨뒤로 부활시키던 것 방지, 전체 트리 탐색 후 있으면 생성 금지)·뷰어 우클릭/휠버튼 pan·확장 manifest 1.3.0 → v1.6.4 북마크 추가 완료 팝업(동기화 시 뒤쪽 생성 안내)·확장 페이지 열기 수정(URL 단독 전달+--load-extension, 빈 창만 열리던 버그)·익스텐션 모달 초보자 개편(①②③ 단계 구조)·뷰어 휠 줌(커서 기준점, 노치당 10%) → v1.6.5 휠 줌 감도 절반(5%)·익스텐션 모달 완전 수동 설치서화(주소/경로 클립보드 복사 유지)·⚙ 설정(규칙 추가 버튼 토글 기본 끄기+매뉴얼 PDF 뷰어, pdfkit으로 resources/manual.pdf 생성·번들) → v1.6.6 시작화면 등록카드 제거(오해 소지)·휠 줌 감도 사용자 설정(기본 1.7%=이전의 1/3, 슬라이더)·시작 프로그램 등록/삭제(HKCU Run, 이미 등록 시 "등록 되어 있습니다." 알림, portable 원본 경로 등록) → v1.6.7 매핑 화면 렌더링 개편 — MHTML 리소스 로컬 해석 정밀화(&amp; 엔티티 URL 매칭, CSS 파트 내부 url()/@import 재작성으로 로컬 파트/절대 URL 해석) + 매핑 모드 최소 렌더 폭 1280px → v1.6.8 네이버 장바구니 규칙(shopping.naver.com/cart, naver-cart — builtin 순서 naver 우선) · charset 스마트 디코딩(smartDecode 전 경로, /html 수신 메타 utf-8 재기록, serveDoc 파트 변환) · 북마크 중복 근본 수정(안정 bookmarkGuid 재사용 + bookmark-info 캐노니컬 57330 고정 + 확장 1.4.0 전 폴더 중복·포트 변종 정리) → v1.6.9 북마크 부활 재발 대응 — Bookmarks.bak 동시 기록, 브라우저 완전 소멸 확인 후 쓰기 + read-back 검증 재시도, 동기화 중복 사전/사후 감지 → 북마크 관리자 열기 안내(파일 삭제는 동기화 tombstone이 안 되므로 브라우저 삭제로 1회 수렴), 재시작 --load-extension → v1.6.10 "삭제 직후 버튼" 함정 방지 — 관리자 삭제 직후(브라우저 실행 중 + 직전 있던 프로필이 0개) 버튼 누르면 tombstone 업로드 전에 크롬이 죽어 복제본이 재전송되므로 "2~3분 대기 후 직접 닫고 재시도" 경고 추가, 쓰기 후 bookmarkCounts 저장. E2E 매핑 프레임 선택 정밀화(picker=1)·shoping_cart 샘플 변동 대응 → v1.6.11 확장 무한 생성 루프 사고 수정 — 재시작 --load-extension 완전 제거(Chrome 153이 실제 로드했었음), 확장 1.4.1 heal 생성 금지·이동/중복제거 전용 재작성 + 회로차단기(8회/60초) → v1.6.12 롯데마트 제타 규칙(data-test 기반, 정답 대조 완료) · 캡처 스타일 누락 수정(확장 1.4.2+북마크릿: adoptedStyleSheets를 DOM style로 박제 후 캡처 — saveAsMHTML의 CSSOM 누락 대응) · 매핑 UX(2개 이상 상품 안내 + 상품 개수 입력·count-selector IPC 행 매칭 검증) → v1.6.13 문서 ID 소문자 생성(대문자→hostname 강제 소문자화로 app-mhtml 무작위 404 — 실증: 롯데 캡처 렌더링 renderCheck로 검증, 스타일시트 3종 2,148룰·Pretendard·가로nav 적용 확인) · 매핑 행 다중 선택·nth-of-type 제거 일반화(한 품목 규칙의 품목 누락 방지) · E2E 강화(cssPartCheck·renderCheck·E2E_SHOT·E2E_DIR 우선) → v1.6.14 행/열 구분 선택 매핑 — 열(표 가로 나열) 쇼핑몰 규칙 지원(orientation:'column' 스키마·extractItemsByColumn·picker 셀 좌표·count-selector 열 모드), 매핑 ①단계 행/열 라디오 → **v1.7.0 장바구니 V체크 캡처 전면 지원 — 13몰 정답 대조 전수 통과(analysis/test-cart-rules.js): 신규 10규칙(kyobo-cart·aladin·gmarket-cart·st11-cart·auction-cart·icecream-cart·alphamall-cart·yes24-cart·officedepot·daisomall) + naver-cart(aria-checked 필터)·coupang(data-selected∩input[checked])·dreamdepot(기본배송비 표시값) 수정, 엔진 확장(perItem 배송비 행·shipping.first·qtyFromUnit), V체크 소실 몰 안내 팝업(RULE_CART_NOTICES), 다이소는 label.is-checked 클래스로 필터링 성공** → **v1.7.1 캡처 시점 V체크 박제 — 모든 쇼핑몰 장바구니에서 V체크한 상품만 추출: 익스텐션(1.4.3)·북마크릿이 캡처 직전 input.checked 프로퍼티를 data-arge-checked 속성으로 박제(saveAsMHTML/outerHTML은 프로퍼티를 직렬화하지 않아 G마켓 카트 0건→거부 팝턴 사고 수정), 엔진 checkedOnly{sel,legacySel}(스탬프 우선→수동저장 checked 속성→상태 없으면 전체 추출 폴백+안내), 12몰 카트 규칙 적용·카트 몰 0건 거부 메시지, bookmarks.js 옛 북마크릿 URL 변종 교체(guid 보존)** → v1.8.0 부분저장 감지(verifyCount: 네이버 6→4·G마켓 7→4 체크 누락 캡처 경고)·PenPDF 라이트 테마 전면 개편(보라 #5B4DFB 포인트·흰 카드·빈 상태 SVG) → v1.9.0 확장 1.5.0 캡처 방식 전환(saveAsMHTML 행 드랍 실측 → live outerHTML + checked 속성 동기화, /html 경로)·북마크릿 checked 속성 동기화·gmarket-cart 가격 match:last(할인가·쿠폰 실제가 채택) → v1.10.0 엔진 units 스키마(G마켓 복수 옵션 라인을 독립 품목으로 분리 — 사용자 교정: 행 드랍이 아니라 옵션 라인)·확장 캡처 전 스크롤 렌더(목록 가상화 방어)+role=checkbox aria 스탬프·매핑 상품 개수 N슬롯(행/상품명/수량/주문금액/배송비 각 N번 클릭+공통 선택자 도출) → v1.10.1 확장 1.5.1 네이버 창(window) 가상화 근본 수정 — 사용자 실측(4개 중 1·4번째 체크 시 1건만 추출, 뷰포트 창 ~2행만 DOM 유지): 캡처 전 스크롤 구간별로 목록 컨테이너(반복 자식 구조)의 행 outerHTML을 수집했다가 캡처 직전 누락 행을 삽입(스티치)한 뒤 스탬프를 다시 찍어 outerHTML 캡처 — 창 밖 행도 모두 포함 → v1.10.2 확장 1.5.2 네이버 캡처 재작성(실제 장바구니 라이브 검증) — 네이버 카트는 뷰포트 ~2행만 DOM에 유지하고 스크롤 시 같은 노드에 다른 상품을 재활용 렌더 + 조상 컨테이너 노드까지 매번 재생성(노드 참조 스티치 불가 확정). 캡처 전 스크롤 구간마다 체크박스 앵커(input/[role=checkbox]의 조상 중 텍스트 40자↑·가격 요소 포함 최소 조상)로 행을 수집, 그룹 간 포함관계로 최내츱(카드) 그룹만 남긴 뒤 현재 DOM에 없는 행을 문서 끝 숨김 컨테이너(data-arge-rows, display:none)에 추가하고 스탬프를 다시 찍어 outerHTML 캡처 — cheerio 엔진이 문서 전체에서 rowSelector를 찾아 숨김 행도 그대로 추출. 라이브 검증: DOM 2행 → 4행 복원, 앱 엔진 4품목 추출(부분저장 경고 없음) 확인 → v1.10.3 확장 1.5.3 캡처 500ms 안정화 대기(네이버·쿠팡 늦은 렌더 누락 방지)·11st 장바구니 체크박스 prefix(bcktSeq_B_1 누락)+주문서 rowSelector 상품 블록화(그룹 내 복수 상품)·엑셀 배송비 금액별 유지 테스트 확정·뷰어 iframe 스크롤바 숨김(이중 스크롤 해소) → v1.11.0 e마트몰(SSG) 2규칙 등록(장바구니 emartmall-cart·주문서 emartmall)·추출결과 표 개편(쇼핑몰 병합 열·글자 1.5배+설정 지정·열 드래그 폭 조절)·확장 1.5.4 네이버 25% 축소 순간 캡처(원복 후 직렬화) → v1.12.0 확장 1.5.5 캡처 순서 재배열(25% 축소 300ms → 축소 상태 저장 → 즉시 복원, zoom 인라인 제거)·e마트몰 장바구니 배송비(viewAmt_shppcst 라벨 합산)·추출결과 중복삭제 버튼(첫 세트 유지)·빈 화면 안내 문구·매핑 슬롯 ✕(클릭 실수 복구) → v1.13.0 extension_auto(version2) 실행기 버튼+extraResources 포함·네이버 추가상품 라인 units 분리(행 수 기준 감지 조정)·e마트몰 추천배제(:has(cbCartId))+배송비 라벨 합산·확장 살짝 스크롤 캡처(꼬리 잘림 방지) → v1.14.0 version2 실행기 경로 수정(extraResources 누락 해결)·G마켓 감지 제거(사용자 확인: 카운터 불일치 정상)·e마트몰 주문서 전용 전환(장바구니 거부 팝업)·네이버 축소 !important 스타일 태그+1000ms 후 축소 상태 저장·lazy 이미지 src 교체·뷰어 measure 600ms 주기 재보고 → v1.15.0 익스텐션 모달 자동 설치 전용화(version1 수동 안내 삭제)·확장명 품의캡처(manifest)·네이버 주문서 전용(장바구니 거부 팝업)·설치 도구 config.json에 확장 경로 프리셋(%APPDATA% extension) → v1.16.0 네이버 채널 구분(meta arge-channel: 확장=정상 추출, 북마크릿/수동=거부 팝업)·G마켓 배송비 perFee 개별 행(합산 금지 — 사용자 요청)

## 8. 사용자 협업 패턴

- 응답은 간결한 한국어, 표/불릿 선호
- 사용자 개입 필요 시(앱 종료 요청 등) **question 도구** 적극 활용 — 즉시 응답해줌
- 모든 변경은 "단독 exe 재생성 → 루트 배포 파일 교체"까지 완료해야 작업 완료로 간주
