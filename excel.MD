# excel.MD — 「기존 엑셀 불러오기」 기능 개편 기술 명세 (v2, as-built 검증판)

> 대상 프로그램: **자동 품의 요구 생성기** (Electron + React/Zustand + Tailwind v4, 좌측 MHTML 뷰어 / 우측 추출 결과 테이블)
> 작성일: 2026-09-14 · 개정: 2026-09-14 (v2 — 실제 소스 검증 후 가정 전면 교정)
> 용도: z.ai coding plan(AI 코딩 에이전트)에 그대로 전달하는 구현 지시서

---

## 0. 작업 요약 (TL;DR)

| # | 요구사항 | 결과 |
|---|---|---|
| 1 | 「기존 엑셀 불러오기」 클릭 시 **우측 "추출 결과" 테이블에 엑셀 행이 자동으로 채워져야** 한다 | 신규 구현 |
| 2 | 좌측 뷰어 영역에 **불러온 엑셀 내용을 시각적으로 표시**해야 한다 | 신규 구현 — **HTML 그리드 렌더링 방식** 채택 (이미지 캡처 방식은 비채택, 이유는 §4 참조) |
| 3 | 하단 「**엑셀에 누적 저장하기**」 버튼 **삭제** | 삭제 + 저장 흐름 단일화 (§6) |

새 사용 흐름:
```
[기존 엑셀 불러오기] → 우측 테이블에 기존 품목 자동 로드 (좌측에 엑셀 탭 표시)
      ↓
[MHTML 폴더/파일 열기] → 신규 품목이 기존 행 뒤에 append
      ↓
[엑셀로 저장] (기존 "다른 경로에 저장" 버튼을 단일 저장 버튼으로 승격)
```
즉, "누적 저장"이 하던 일(기존 파일에 이어 붙이기)은 **불러오기 → 추가 → 저장**으로 대체된다.

---

## 1. 현재 상태 (As-Is) — 실제 소스 검증 완료 (v2)

### 1.1 화면 구성 (실측)
- **상단 툴바(Header.jsx)**: `MHTML 폴더 열기` / `MHTML 파일 열기` / `기존 엑셀 불러오기` / `새 쇼핑몰 규칙 추가` / `북마크바 추가` / `익스텐션 추가` / `규칙 관리`
- **좌측 패널(Viewer.jsx)**: 문서 탭(`fileName` 표시) + 확대/축소(25~300%) + 쇼핑몰 규칙 선택 드롭다운. 각 탭은 `app-mhtml://` 프로토콜 iframe으로 MHTML/HTML 렌더링.
- **우측 패널(Grid.jsx)**: "추출 결과 (클릭하여 수정)" 테이블 + `＋ 행 추가` 버튼. 각 행 우측에 `×` 삭제.
- **하단 상태바(StatusBar.jsx)**: `MHTML N개 | 품목 N건 | 총액 N원 | 엑셀: <경로> (기존 N행)` + `엑셀에 누적 저장하기`(파랑), `📂 다른 경로에 저장`(흰색).

### 1.2 우측 테이블 컬럼 (실측 — 9열)
| 컬럼 | 표시 | 비고 |
|---|---|---|
| 순번 | 렌더 시 1부터 계산 (`let no = 0` 증가) | 배송비 행은 순번 대신 🚚 표시 |
| 품목명 / 규격 / 단위 / 수량 / 예상단가 | 행 데이터 | 셀 클릭 편집(EditableCell) |
| 총액 | `수량 × roundedPrice` 화면 계산 | 저장되지 않는 표시 전용 값 |
| 비고 | 행 데이터 `note` | **UI에만 존재, 현재 저장 형식에는 없음** |
| × | 행 삭제 | |

### 1.3 실제 행 스키마 (docstore.js → store.js `withKeys`)
```ts
interface Row {           // 실제 필드명 — 신규 기능도 이 이름을 따름
  key: string;            // r1, r2… (withKeys에서 부여, 편집/삭제 키)
  name: string; spec: string; unit: string;   // "개" | "식" | 기타
  qty: number;
  unitPrice: number;      // 추출 원가(계산값)
  roundedPrice: number;   // 예상단가 = roundUpToTen(unitPrice) — UI/저장 기준
  isShipping: boolean;
  note: string;           // 수동 추가 행 등에서 사용
  // 신규 추가: source: 'mhtml' | 'excel' | 'manual', sourceRef?: { sheetName, excelRowIndex }
}
```

### 1.4 엑셀 입출력 실측 (lib/excel.js — SheetJS)
- **라이브러리: SheetJS(`xlsx`)** — exceljs 아님. `.xls`(BIFF8) 읽기/쓰기 **네이티브 지원** (`bookType:'biff8'`).
- **저장 형식(실제)**: 시트 `품목내역`, 헤더 **5열** `내용 | 규격 | 단위 | 수량 | 예상단가` — 순번·총액·비고 열 없음, 합계 행 없음, 수식 없음(값만 기록). `.bak` 백업 후 기록.
- **배송비 저장 규칙(v1.5.0, 유지 필수)**: `appendRows()`의 `aggregateShipping()`이 `isShipping` 행을 **가격별로 합산**한다 (`3000원×1 + 3000원×2 + 4000원×1` → `배송비/식/3/3000` + `배송비/식/1/4000`, 일반 품목 뒤에 가격 오름차순).
- **현재 읽기(`readExcelRows`)**: 첫 10행에서 `내용` 또는 `품목명` 포함 행을 헤더로 탐지, 이후 행을 배열로 반환 — **우측 테이블에는 반영하지 않고 상태바 `기존 N행` 카운트만 표시**. → 이번 작업의 핵심 개선 대상.
- **IPC 채널(실측)**: `read-excel` / `pick-excel` / `append-excel` / `save-excel-as` (preload `readExcel`/`pickExcel`/`appendExcel`/`saveExcelAs` 매핑). 저장 경로 기본값은 `settings.json`의 `excelPath`.

---

## 2. 목표 상태 (To-Be) 요구사항

### R1. 엑셀 불러오기 → 우측 테이블 자동 반영
1. 「기존 엑셀 불러오기」 클릭 → 파일 선택 다이얼로그(`.xls`, `.xlsx`).
2. 선택한 파일을 파싱해 **품목 행 목록**으로 변환한다 (§3 파싱 규칙). 파싱은 기존처럼 메인 프로세스에서.
3. 우측 테이블에 이미 행이 있으면 확인 다이얼로그를 띄운다:
   - `교체` : 기존 행 모두 삭제 후 엑셀 행으로 대체
   - `뒤에 추가` : 기존 행 뒤에 엑셀 행 append
   - `취소`
   - 우측 테이블이 비어 있으면 다이얼로그 없이 바로 로드.
4. 로드된 행은 `source:"excel"` 표시. MHTML 추출 행은 `source:"mhtml"`, `＋ 행 추가`로 만든 행은 `source:"manual"`.
5. 엑셀에서 로드한 행은 **"엑셀" 가상 문서**(`docs`에 `id:'excel-…'` 문서로 추가하거나 동등 구조)에 담아 기존 렌더링·상태바 집계 로직(`StatusBar`의 `docs.reduce`)을 그대로 재사용한다. 순번은 기존처럼 렌더 시 재계산(배송비 제외).
6. 로드된 행도 기존 행과 동일하게 **클릭 편집 / 삭제(×) / 행 추가** 가능.
7. 이후 MHTML을 열어 추출되는 신규 행은 **항상 테이블 마지막에 append**(기존 동작 유지). 단, v1.5.1부터 신규 문서가 좌측 뷰어에 자동 선택되므로 엑셀 가상 문서가 이미 선택된 경우에도 **새 캡처가 열리면 그 문서로 선택 전환**(기존 동작)하고 우측 테이블은 전체 문서 행을 이어서 표시(기존 동작).

### R2. 좌측 패널에 엑셀 내용 표시
1. 엑셀 로드 시 좌측 탭 영역에 새 탭 **`[엑셀] <파일명>`** 을 추가하고 자동 활성화한다.
2. 탭 내용은 엑셀 시트를 **읽기 전용 HTML 그리드**로 렌더링한다 (§4). 기존 `app-mhtml://` iframe과 달리 일반 DOM 렌더링.
3. 기존 확대/축소 컨트롤이 이 탭에도 동일하게 동작 (`transform: scale`, store의 `zoom` 공유).
4. 우측 테이블의 `source==="excel"` 행을 클릭하면 좌측 엑셀 탭의 해당 원본 행이 하이라이트되고 스크롤된다 (`excelRowIndex` 보존). — 우선순위: 중.
5. 시트가 여러 개인 경우: 품목 헤더가 탐지된 시트를 기본 표시, 탭 내부 상단에 시트 선택 드롭다운 제공.
6. 탭 닫기(×) 시 좌측 뷰만 제거하며 우측 행은 유지한다(안내 토스트 1회).

### R3. 「엑셀에 누적 저장하기」 버튼 삭제 및 저장 흐름 단일화
1. 하단 상태바의 `엑셀에 누적 저장하기` 버튼과 store의 `saveToExcel()` 제거. `append-excel` IPC는 **삭제하지 않고 유지**(E2E가 사용)하되 renderer에서 더 이상 호출하지 않음. `saveExcelAs()`는 유지.
2. `📂 다른 경로에 저장` 버튼을 **`엑셀로 저장`** 으로 이름 변경하고 주(primary) 버튼 스타일로 승격.
3. `엑셀로 저장` 동작(기존 `save-excel-as` IPC 동작 준용):
   - 저장 다이얼로그 표시, **기본 경로 = 마지막으로 불러온/저장한 엑셀 경로**(`settings.excelPath`, 없으면 기존 `defaultXlsPath()`).
   - 기존 파일 선택 시 기존 행 뒤에 append(기존 동작 유지), 새 파일이면 헤더 생성 후 기록. OS 표준 덮어쓰기 확인 제공.
   - 우측 테이블의 **현재 전체 행**(excel + mhtml + manual)을 기록하며, 저장 컬럼은 **기존 5열 형식(`내용|규격|단위|수량|예상단가`) 그대로**, 배송비는 §1.4의 가격별 합산 규칙이 자동 적용됨(`appendRows` 내부).
   - 저장 성공 후 `settings.excelPath` 갱신 + 좌측 엑셀 탭 내용 재렌더링(§7-8).
4. 단축키 `Ctrl+S` → `엑셀로 저장`.

---

## 3. 엑셀 파싱 규칙 (읽기)

### 3.1 헤더 행 탐지
- 시트 상단 1~30행을 스캔해 아래 별칭 중 **3개 이상 매칭되는 첫 행**을 헤더로 판정한다 (제목 행·결재란·병합 셀이 위에 있어도 대응). 기존 `readExcelRows`의 10행 제한을 30행으로 확장.
- 공백/개행 제거, 대소문자 무시 후 비교.

| 표준 필드 | 인식 별칭 |
|---|---|
| `name` 품목명 | **내용(기존 저장 헤더 — 필수 별칭)**, 품목명, 품명, 품목, 상품명, 물품명 |
| `spec` 규격 | 규격, 사양, 옵션, 모델 |
| `unit` 단위 | 단위 |
| `qty` 수량 | 수량, 량, 개수 |
| `unitPrice` 예상단가 | 예상단가, 단가, 예정단가, 추정단가 |
| `note` 비고 | 비고, 메모, 참고 |
| `seq` 순번 | 순번, 번호, No, NO., 연번 (탐지에만 사용, 값은 버림) |
| `amount` 총액 | 총액, 금액, 합계금액, 예상금액 (있으면 무시 — 항상 qty×roundedPrice 재계산) |

- 헤더를 못 찾으면: 에러 토스트 `"품목 표(품목명/수량/단가 컬럼)를 찾지 못했습니다."` 후 테이블 반영 중단. 단, 좌측 엑셀 탭 표시는 그대로 수행.

### 3.2 데이터 행 판정
- 헤더 다음 행부터 아래로 스캔.
- **종료 조건**: 연속 3행 완전히 빈 행이거나 `name`이 `^\s*(총\s*)?(합|소)?계\s*$` 일치 시 그 행부터 무시.
- **건너뛰기**: `name`이 비어 있고 `qty`·`unitPrice`도 모두 비어 있는 행.
- **배송비 행**: `name`에 `배송비|배송료|택배비|운임` 포함 또는 `unit==="식"` && `name`에 `배송` 포함 → `isShipping:true`, `unit:"식"`, 순번 카운트 제외.

### 3.3 값 정규화 → 실제 스키마 매핑
```text
qty        : "10개" → 10 (숫자 외 제거 parseInt). 실패/빈값 → 1
roundedPrice: "23,970원" → 23970. 실패/빈값 → 0
unitPrice  : roundedPrice와 동일값 설정 (엑셀 출발 행은 올림 전 원가를 알 수 없음)
unit       : 빈값 → 배송비면 "식", 아니면 "개"
name/spec/note : trim, 개행→공백
key        : 기존 withKeys()로 자동 부여 (r1, r2…)
```
- SheetJS `sheet_to_json(ws, {header:1, raw:false, defval:null})` 사용 — 수식 셀은 cached 값을 문자열로 반환.
- 날짜 셀 방지: Date 타입이면 `YYYY-MM-DD` 문자열로 변환.
- **주의(라운드트립)**: 저장 시 배송비가 가격별 합산되므로, 합산된 형태(`배송비/식/3/3000`)를 다시 불러오면 `qty=3, isShipping` 행 1개로 로드되어야 한다 (T6).

### 3.4 행 객체 스키마 (§1.3 실제 필드 + 신규 2종)
```ts
interface Row {
  key: string; name: string; spec: string; unit: string;
  qty: number; unitPrice: number; roundedPrice: number;
  note: string; isShipping: boolean;
  source: 'mhtml' | 'excel' | 'manual';        // 신규 — 기존 행은 마이그레이션 시 'mhtml' 취급
  sourceRef?: { sheetName?: string; excelRowIndex?: number };  // 신규 — 좌↔우 하이라이트 연동용
}
```

---

## 4. 좌측 패널 엑셀 표시 — 방식 비교 및 채택안

| 방식 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **A. HTML 그리드 렌더링** (파싱한 셀 데이터를 `<table>`로 렌더) | 의존성 추가 없음(SheetJS 이미 사용), 확대/축소·행 하이라이트·복사 가능, 로딩 빠름 | 조건부 서식·차트 미표시 (품의용 표에는 불필요) | **✅ 채택** |
| B. 엑셀 → 이미지 캡처 (LibreOffice headless 등) | 원본 외형 100% | 설치 의존, 3~10초, 배포 용량↑, 행 연동 불가 | ❌ |
| C. 스프레드시트 컴포넌트 (Handsontable 등) | 실제 시트 느낌 | 번들 大, 라이선스, 편집 이중화 | ❌ |

### 4.1 A안 렌더링 명세
- 컴포넌트: `Viewer.jsx` 내 문서 탭 렌더링 분기에 `kind==='excel'` 추가(또는 `ExcelView.jsx` 신규). 스크롤 독립.
- 상단 바: 시트 선택 드롭다운(시트 2개 이상) + 파일명 + `읽기 전용 · 수정은 우측 표에서` 텍스트.
- 그리드: 좌측 엑셀 행 번호(1,2,3…), 상단 열 문자(A,B,C…) 표시. 셀 폰트 `Malgun Gothic 12px`, 테두리 `1px solid #d9d9d9`, 헤더 배경 `#f3f4f6`.
- 열 너비: SheetJS `ws['!cols']`의 `wpx` 사용, 없으면 내용 최대 길이 자동(최소 40px·최대 400px).
- 병합 셀(`ws['!merges']`)은 `rowspan/colspan` 적용.
- 셀 정렬: 숫자 우측·천단위 콤마, 문자 좌측. 배경색/굵기는 표준 방식으로 반영 가능하면만(선택).
- 헤더 탐지 행에 `class="detected-header"`, 데이터 행에 `data-excel-row="{n}"`.
- 성능: 최대 2,000행 × 50열 렌더, 초과 시 상단 안내.
- 확대/축소: store `zoom` 공유 적용.
- 하이라이트: 우측 행 클릭 → `data-excel-row` 매칭 `tr.active-row`(배경 `#fff4cc`) + `scrollIntoView({block:'center'})`.

### 4.2 최소 코드 스케치 (SheetJS 기준 — 실제 사용 라이브러리)
```js
// main — 기존 read-excel IPC 확장 (lib/excel.js)
import * as XLSX from 'xlsx'

export function loadExcelFull(filePath) {
  const wb = XLSX.readFile(filePath, { cellDates: false })
  const sheets = wb.SheetNames.map(name => {
    const ws = wb.Sheets[name]
    return {
      name,
      rows: XLSX.utils.sheet_to_json(ws, { header: 1, raw: false, defval: null }),
      merges: ws['!merges'] || [],
      cols: ws['!cols'] || []
    }
  })
  const picked = pickSheetAndParse(sheets)      // §3 규칙 — 헤더 탐지 → Row[] 변환
  return { filePath, sheets, ...picked }        // items, headerRow, sheetName
}
```
```js
// renderer — loadExcel 흐름
const path = await window.api.pickExcel()
if (!path) return
const res = await window.api.loadExcelFull(path)          // 신규 IPC
if (res.error) { toast(res.error, 'err'); /* 엑셀 탭은 표시 */ }
const mode = existingRows.length ? await confirmReplaceOrAppend() : 'append'
if (mode === 'cancel') return
applyExcelRows(mode, res.items)                            // 교체/추가 + source:'excel'
openExcelTab(res)                                          // 좌측 [엑셀] 탭
```

---

## 5. 엑셀 저장 형식 (쓰기) — **기존 형식 그대로 유지**
- 시트 `품목내역`, 헤더 **5열** `내용 | 규격 | 단위 | 수량 | 예상단가`, 값만 기록(BIFF8 `.xls`, 수식·합계 행 없음 — 기존 동작).
- 순번·총액·비고는 저장하지 않는다(불러오기 시 순번 재계산, 총액은 화면 계산).
- 배송비: `aggregateShipping()` 가격별 합산 유지(§1.4). 합산 후 배송비 `name`은 `배송비`, `unit`은 `식`.
- 라운드트립 보장: **저장 → 불러오기 → 저장** 결과의 데이터 영역이 동일해야 한다(T6). 합계 행이 없으므로 중복 우려 없음.

---

## 6. UI 변경 목록

| 위치 | 변경 |
|---|---|
| 하단 상태바 | `엑셀에 누적 저장하기` 버튼 **삭제** (`saveToExcel` 포함) |
| 하단 상태바 | `📂 다른 경로에 저장` → **`엑셀로 저장`** (primary 스타일, `Ctrl+S`) |
| 하단 상태바 | `엑셀: <경로> (기존 N행)` 표기 유지, 마지막 불러온/저장한 경로로 갱신 |
| 좌측 탭 | `[엑셀] 파일명` 탭 유형 추가. MHTML 탭과 구분(시트 아이콘 📑 등) |
| 좌측 툴바 | 엑셀 탭 활성 시 "쇼핑몰 규칙" 드롭다운 disabled |
| 우측 테이블 | `source==='excel'` 행 회색 마커(선택). 행 클릭 → 좌측 하이라이트 (§4.1) |
| 다이얼로그 | 엑셀 로드 시 `교체 / 뒤에 추가 / 취소` 3버튼 확인창 |

---

## 7. 엣지 케이스
1. **`.xls`(BIFF8)**: SheetJS가 네이티브 지원 — 별도 폴백 불필요 (기존 저장 형식이 .xls).
2. **파일이 Excel에서 열려 있어 잠김**: 읽기는 가능. 저장 시 `EBUSY` → `"파일이 다른 프로그램에서 사용 중입니다. 닫고 다시 시도하세요."` 토스트.
3. **헤더가 2줄(병합)인 표**: 별칭 3개 이상 매칭되는 행을 헤더로, 그 위 행은 무시.
4. **숫자가 문자열로 저장된 셀**(`'23970`): §3.3 정규화로 처리.
5. **동일 파일 재불러오기**: 같은 경로의 엑셀 탭이 있으면 탭 재사용(내용 갱신), 교체/추가 다이얼로그 정상 표시.
6. **매우 큰 파일**: 메인에서 비동기 파싱, 렌더러 스피너, 그리드 2,000행 제한.
7. **엑셀 로드 후 MHTML 열기**: 신규 행 append, 순번 전체 재계산. 좌측 선택은 새 문서로 전환(기존 v1.5.1 동작 유지).
8. **저장 시 좌측 엑셀 탭 갱신**: 저장 후 엑셀 탭 내용을 저장된 파일 기준 재렌더.
9. **배송비 합산 재로드**: 합산 저장된 `배송비/식/N/단가` 행은 불러올 때 `qty=N`인 배송비 행 1개로 복원(다시 저장해도 동일 — 라운드트립).

---

## 8. 수용 기준 / 테스트 시나리오
| ID | 시나리오 | 기대 결과 |
|---|---|---|
| T1 | 빈 상태에서 11개 품목 + 배송비 2행이 든 .xls 불러오기 | 우측 13행, 순번 1~11(배송비 제외), 상태바 `품목 11건` 총액 갱신, 좌측 `[엑셀]` 탭 활성 |
| T2 | 행이 있는 상태에서 불러오기 → `뒤에 추가` | 기존 행 유지 + 엑셀 행 append, 순번 연속 |
| T3 | 행이 있는 상태에서 불러오기 → `교체` | 기존 행 제거, 엑셀 행만 남음 |
| T4 | 엑셀 로드 후 MHTML 파일 열기 | 추출 행 마지막 추가, 총액·건수 갱신 |
| T5 | 우측 엑셀 출처 행 클릭 | 좌측 엑셀 탭 해당 행 하이라이트 + 스크롤 |
| T6 | 저장 → 불러오기 → 저장 (라운드트립) | 데이터 영역 동일. 배송비는 가격별 합산 형태로 동일 재현 (예: 3000원×3 → 1행 qty 3) |
| T7 | 제목 3줄 + 결재란 병합 셀 양식 | 헤더 자동 탐지 성공 (별칭 3개 매칭) |
| T8 | 품목 표가 없는 엑셀 | 에러 토스트, 우측 변화 없음, 좌측 탭은 표시 |
| T9 | 하단 상태바 | `엑셀에 누적 저장하기` 없음, `엑셀로 저장` 단일 버튼, `Ctrl+S` 동작 |
| T10 | 확대/축소 | 엑셀 탭에서도 동작 |
| T11 | 기존 기능 회귀 | MHTML 추출·배송비 합산 저장·몰별 거부 팝업 등 v1.5.x 기능 무손상 (E2E 전수 통과) |

---

## 9. 구현 순서 (권장)
1. ~~소스 탐색~~ — **완료(v2): 본 문서 §1이 실측값.** 프레임워크 Electron+React/Zustand, 엑셀 SheetJS(biff8), 행 스키마 §1.3, 저장 5열 형식 확인됨.
2. 메인 `loadExcelFull()` 파서 + §3 규칙 구현, E2E에 T1/T7/T8 케이스 추가(기존 `runE2E`의 excel 블록 확장).
3. 우측 테이블 연동(교체/추가 다이얼로그, `source` 필드, 엑셀 가상 문서, 상태바).
4. 좌측 `excel` 탭 + HTML 그리드 렌더러(§4) + zoom 연동.
5. 좌↔우 하이라이트 연동(§4.1).
6. 누적 저장 버튼/`saveToExcel` 제거, `엑셀로 저장` 승격, `Ctrl+S`.
7. 라운드트립(T6)·회귀(T11) 테스트 — dev E2E + 패키징 exe E2E 모두 통과 후 배포(SUMMARY.MD 버전 갱신 포함).

---

## 10. 비범위 (Out of Scope)
- 좌측 엑셀 뷰에서의 직접 편집 (편집은 우측 테이블 단일 원천 유지)
- 차트/이미지/조건부 서식 재현
- 저장 형식 확장(순번·총액·비고 컬럼 추가, 합계 행) — 기존 5열 BIFF8 형식 유지
