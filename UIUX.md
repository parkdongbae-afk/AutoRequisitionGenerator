# UI/UX 설계서 (as-built)
**업데이트:** 2026-09-23 — v1.8.0 PenPDF 스타일 라이트 테마 개편 반영

## 0. 디자인 테마 (v1.8.0 — 모던 라이트 테마)

- **컨셉:** 화이트/연회색 바탕 + 소프트 인디고/보라 포인트, 흰 카드 컨테이너, 큰 곡률(12~16px), 소프트 섀도우
- **팔레트(styles.css `:root` 토큰):** 앱 배경 `#F4F6F8` · 카드 `#FFFFFF` · 프라이머리 `#5B4DFB`(hover `#4C3DE6`) · 틴트 `#EEEDFE` · 텍스트 `#1E293B`/`#334155`/`#64748B`/`#94A3B8` · 보더 `#E2E8F0`·`#F1F5F9` · 옐로우 틴트 배지 `#FEF3C7`(+`#B45309`) · 그린 틴트 배지 `#E6F4EA`
- **폰트:** Pretendard → Noto Sans KR → Malgun Gothic 폴백
- **공통 카드:** 좌우 패널 `rounded-2xl + shadow 0 4px 12px rgba(0,0,0,0.03)`, 워크스페이스 padding 16px·gap 16px
- **빈 상태:** 뷰어/그리드 각각 파스텔 보라톤 인라인 SVG 아이콘 + 안내 문구(`#64748B`)

## 1. 전체 레이아웃 (Split View 구조)
상단 헤더 + 좌/우 분할 워크스페이스 + 하단 상태바. Tailwind CSS v4.

### 상단 헤더 (Header)
- 흰 배경 + 하단 1px `#E2E8F0` 구획선, padding 12px 20px
- 좌측: 📦 아이콘(보라 틴트 라운드 박스) + "자동 품의 요구 생성기" (18px Bold `#5B4DFB`)
- 버튼 스타일 4종:
  - **그레이 아웃라인**(`#F8FAFC` 배경 + `#E2E8F0` 보더): [MHTML 폴더 열기] [MHTML 파일 열기] [기존 엑셀 불러오기] (+ [새 쇼핑몰 규칙 추가] — 설정에서 표시 토글)
  - **옐로우 틴트**: [⭐ 북마크바 추가] (`#FEF3C7`/`#B45309`)
  - **퍼플 틴트**: [🧩 익스텐션 추가] (`#EEEDFE`/`#5B4DFB`)
  - **미니멀 텍스트**: [🗑 규칙 관리] [⚙ 설정] (`#64748B`, hover `#F1F5F9`)

### 좌측 패널 (캡처 뷰어)
- **일반 모드:** 파일 탭(선택 탭 = 보라 틴트 배경 + 보라 하단 인디케이터, 규칙 미적용 ⚠, ✕ 닫기) / 제어 바(＋확대·－축소·100%·배율 25~300% + 쇼핑몰명·규칙 수동 선택 드롭다운) / 렌더 영역(`app-mhtml://` 원본, pan·휠 줌)
- **매핑 모드:** 캡처 대기 안내(📡) → 캡처 도착 시 "매핑 모드" 보라 틴트 배너. 요소 클릭 시 보라 외곽선 하이라이트
- 선택 문서 행 하이라이트 = 보라 틴트 `#EEEDFE`

### 우측 패널 (추출 결과 그리드)
- 헤더: "추출 결과 (클릭하여 수정)" 13px Bold + [＋ 행 추가] 보라 틴트 라운드 버튼
- 표: 헤더 행 `#F8FAFC` 40px·`#475569`, 행 구분선 `#F1F5F9`, hover `#F8FAFC`, 셀 hover 보라 링
- 열: 순번/품목명/규격/단위/수량/예상단가/총액/비고/삭제 — 셀 클릭 인라인 편집, 규격 자동 생성, 배송비 행 🚚+옐로우 배경, 총액 자동 계산
- 빈 상태: MHTML→엑셀 SVG 아이콘 + "추출된 품목이 없습니다…"

### 하단 상태바 (StatusBar)
- 흰 배경 + 상단 보더, 정보 칩(`#F8FAFC` rounded-full): MHTML N개 · 품목 N건 · 총액 N원 · 엑셀 경로
- 우측: 단가 인상 드롭다운(라운드 보더) + **[엑셀에 저장] CTA** — 보라 솔리드 `#5B4DFB` rounded-[10px], hover `#4C3DE6`+scale 1.02. 선택 파일을 현재 표로 교체(.bak 백업), 배송비 가격별 합산

### 분할바
- 좌/우 드래그 조절(기본 2/3, 더블클릭 복원, 비율 settings 저장), `#E2E8F0` → hover 보라

## 2. 새 쇼핑몰 규칙 추가 (매핑 학습 모달)
우측 절반 배치(오버레이 `bg-slate-900/40`+blur, 흰 카드 `rounded-2xl`). 6단계: ① 행 선택 ② 상품명 ③ 수량 ④ 주문금액 ⑤ 배송비 ⑥ 확인/저장 — 스텝 버튼 활성 = 보라 솔리드, 선택 결과 박스 실시간 표시(성공 그린 틴트/대기 그레이)

## 3. 피드백 (Toasts)
우하단 흰 카드 토스트(rounded-xl + 타입별 좌측 보더 4px): 성공 `#22C55E` / 오류 `#EF4444` / 정보 `#5B4DFB` / 경고 `#F59E0B`. 4초 자동 소멸
- 부분저장 감지(v1.8.0) 시 별도 경고 팝업: "체크 N개 중 캡처에 M개만 저장…" (ALGO.md §2 verifyCount)

## 4. 모달 공통 & 창 규격
- 모달(Extension/Rules/Settings): 오버레이 `bg-slate-900/40`+backdrop-blur, 카드 `rounded-2xl shadow-2xl`, 타이틀 16px Bold `#1E293B`
- 창: 기본 1500×950, 최소 1100×700, 메뉴바 자동 숨김, 타이틀 "자동 품의 요구 생성기"
