# UI/UX 설계서 (as-built)
**업데이트:** 2026-09-13 — 구현 완료 상태 기준

## 1. 전체 레이아웃 (Split View 구조)
화면을 좌/우 50:50으로 분할. 상단 헤더 + 하단 액션 바. 슬레이트 계열 테마(Tailwind CSS v4).

### 상단 헤더 (Header)
- 로고 및 제목: "📦 자동 품의 요구 생성기"
- 기능 버튼: [MHTML 폴더 열기], [MHTML 파일 열기], [기존 엑셀 불러오기], [새 쇼핑몰 규칙 추가]
- 우측: [🌐 브라우저 원클릭 연동] (에메랄드 강조, 클릭 한 번으로 Edge/웨일 확장 바로가기 자동 생성 + 북마크릿 설치 페이지 오픈, 결과 토스트 안내) + [익스텐션 폴더] (수동용 보조)

### 좌측 패널 (캡처 뷰어 / 라이브 브라우저)
- **일반 모드 (캡처 뷰어):**
  - 파일 탭: 캡처 파일 탭 (규칙 미적용 문서는 ⚠ 표시, ✕ 닫기). 탭 툴팁에 원본 URL
  - 제어 바: [＋확대] [－축소] [100%] + 배율 표시(25%~300%), 인식 쇼핑몰명 + 규칙 수동 선택 드롭다운
  - 렌더링 영역: `app-mhtml://` 원본 표시(이미지 포함). **스크롤바 상시 표시**(overflow: scroll) — 확대 시 좌우/상하 이동
- **매핑 모드 (v1.3.0 캡처 대기):** [새 쇼핑몰 규칙 추가] 클릭 → 문서 탭 영역에 대기 안내(📡 캡처 대기 중 + 사용법). 사용자가 본인 브라우저에서 익스텐션/🛒품의캡처 전송 시 캡처 문서가 좌측에 표시되고 "매핑 모드 — 좌측 캡처 문서에서 항목을 클릭하여 지정하세요" 파란 바 표시. 요소 클릭 시 파란 외곽선 하이라이트, 선택자가 우측 모달에 실시간 표시
- *인터랙션:* 탭 클릭 시 우측 패널에서 해당 파일 추출 행이 연한 파란색(#eff6ff)으로 하이라이트

### 우측 패널 (데이터 확인 및 엑셀 제어 영역)
- **데이터 그리드 (엑셀 형태의 표):**
  - 열(Columns): 순번, 품목명, 규격, 단위, 수량, 예상단가, 총액, 비고, 삭제 (+ 서식의 단위 컬럼 반영)
  - **수정 기능:** 셀 클릭 → 인라인 편집 즉시 시작 (Enter 확정 / Esc 취소). 수량·단가는 숫자만 입력
  - **규격 자동 생성:** 품목명/옵션에서 규격 토큰(324g, 355ml, 20개입, 1상자, 30종, 폭 57cm, A4 등)을 자동 추출해 채움. 적합한 규격이 없으면 빈칸 (출처: test_OK 정답 패턴)
  - 배송비 행: 🚚 아이콘 + 노란 배경(#fefce8)으로 구분, 일반 품목과 동일하게 편집 가능
  - 행 삭제: 행 끝 ✕ 버튼
  - 총액 = 수량 × 예상단가(올림가) 자동 계산

### 하단 액션 바 (StatusBar)
- 총 MHTML 파일 수, 총 품목 수(배송비 제외), 총액(원)
- 현재 엑셀 경로 + 기존 행 수 (미지정 시 "기본 경로 사용" 안내)
- [엑셀에 누적 저장하기] 버튼 (파란색 강조, 현재 경로에 누적)
- [📂 다른 경로에 저장] 버튼 (저장 폴더·파일명 직접 지정)

## 2. 새 쇼핑몰 규칙 추가 (매핑 학습 모달)
**우측 절반에 배치** — 좌측 MHTML 뷰어는 클릭·스크롤 가능한 채로 유지되며, 뷰어에서 요소를 클릭하면 선택자가 자동 지정됨.
6단계 스텝퍼 + 진행 상태 패널:
1. **① 행 선택** — 좌측 뷰어에서 상품 행 클릭 (파란 외곽선 하이라이트)
2. **② 상품명** — 행 안에서 상품명 클릭
3. **③ 수량** — 수량 요소 클릭 또는 [건너뛰기]
4. **④ 단가** — 가격 요소 클릭
5. **⑤ 배송비** — 배송비 금액 클릭 또는 [건너뛰기]
6. **⑥ 확인/저장** — 규칙 이름 / 규칙 ID(영문) / URL 패턴 확인 → [규칙 저장하고 이 문서에 적용]

각 선택 결과(샘플 텍스트)가 모달 내 박스에 실시간 표시됨.

## 3. 피드백 (Toasts)
우하단 알림 스택 (4초 자동 소멸): 성공(녹색), 경고(주황), 오류(빨강), 정보(슬레이트)
- 예: "MHTML 3개 로드 (추출 12행)", "규칙 없는 쇼핑몰 1개: 매핑 학습이 필요합니다", "12행 누적 저장 완료 → 경로"

## 4. 창 규격
- 기본 1500×950, 최소 1100×700, 메뉴바 자동 숨김, 타이틀 "자동 품의 요구 생성기"
