const fs = require('fs')
const path = require('path')
const PDFDocument = require('pdfkit')

const out = path.join(__dirname, '..', 'resources', 'manual.pdf')
fs.mkdirSync(path.dirname(out), { recursive: true })

const W = 595.28
const M = 56
const CW = W - M * 2

const doc = new PDFDocument({ size: 'A4', margins: { top: 56, bottom: 56, left: M, right: M } })
doc.pipe(fs.createWriteStream(out))

doc.registerFont('kr', 'C:/Windows/Fonts/malgun.ttf')
doc.registerFont('kb', 'C:/Windows/Fonts/malgunbd.ttf')

const H1 = (t) => {
  ensure(40)
  doc.font('kb').fontSize(16).fillColor('#1e293b').text(t, M, doc.y, { width: CW })
  doc.moveDown(0.4)
  doc.moveTo(M, doc.y).lineTo(W - M, doc.y).lineWidth(1.2).strokeColor('#2563eb').stroke()
  doc.moveDown(0.7)
}
const H2 = (t) => {
  ensure(34)
  doc.font('kb').fontSize(12.5).fillColor('#1d4ed8').text(t, M, doc.y, { width: CW })
  doc.moveDown(0.35)
}
const P = (t, opts = {}) => {
  doc.font('kr').fontSize(10.5).fillColor('#334155').text(t, M, doc.y, { width: CW, lineGap: 3.5, ...opts })
  doc.moveDown(0.25)
}
const STEP = (n, t) => {
  ensure(20)
  doc.font('kb').fontSize(10.5).fillColor('#2563eb').text(`${n}.`, M, doc.y, { width: 20, continued: false })
  const y0 = doc.y - doc.currentLineHeight()
  doc.font('kr').fillColor('#334155').text(t, M + 20, y0, { width: CW - 20, lineGap: 3.5 })
  doc.moveDown(0.2)
}
const BOX = (title, lines) => {
  ensure(40)
  const y0 = doc.y
  doc.font('kb').fontSize(10.5).fillColor('#065f46').text(title, M + 10, y0 + 8, { width: CW - 20 })
  let y = doc.y
  for (const l of lines) {
    doc.font('kr').fillColor('#064e3b').text(l, M + 10, y, { width: CW - 20, lineGap: 3 })
    y = doc.y
  }
  const h = y + 8 - y0
  doc.lineWidth(1).strokeColor('#6ee7b7').roundedRect(M, y0, CW, h, 6).stroke()
  doc.y = y0 + h
  doc.moveDown(0.5)
}
const CODE = (t) => {
  doc.font('kb').fontSize(10.5).fillColor('#1e3a8a').text(t, M + 24, doc.y, { width: CW - 48 })
  doc.moveDown(0.3)
}

function ensure(h) {
  if (doc.y + h > 841.89 - 56) doc.addPage()
}

doc.font('kr')
doc.fontSize(21).fillColor('#0f172a').font('kb').text('자동 품의 요구 생성기 — 사용 매뉴얼', M, 70, { width: CW, align: 'center' })
doc.moveDown(0.3)
doc.font('kr').fontSize(11).fillColor('#64748b').text('쇼핑몰 주문/장바구니 화면을 캡처해 품의 서식 엑셀에 자동 저장하는 프로그램', M, doc.y, { width: CW, align: 'center' })
doc.moveDown(1)

H1('1. 프로그램 개요')
P('쇼핑몰의 주문서/장바구니 화면을 캡처하면 품목명·규격·수량·예상단가·배송비를 자동으로 추출하고, 품의 서식 엑셀(품목내역(통합).xls)에 저장합니다. G마켓, 네이버쇼핑, 쿠팡, 옥션, 11번가, 교보문고, 예스24, 알라딘, 티처몰, 다이소몰, e마트몰(SSG), 롯데마트 제타, 엘레파츠, IC114 등 34개 몰의 규칙이 내장되어 있으며, [설정]의 규칙 업데이트 기능으로 새 규칙이 자동으로 추가됩니다.')
P('장바구니 화면에서는 체크(V)한 상품만 골라 추출합니다. 캡처 방법은 두 가지입니다. ① 브라우저 확장 프로그램(품의캡처) 아이콘 클릭 ② 북마크바의 [🛒품의캡처] 클릭. 어느 쪽이든 프로그램이 자동으로 받아 표시합니다.')
doc.moveDown(0.3)

H1('2. 시작하기 — 북마크바에 품의캡처 추가')
STEP(1, '프로그램 상단의 [⭐ 북마크바 추가] 버튼을 클릭합니다.')
STEP(2, '브라우저와 프로필을 선택합니다(기본: 설치된 모든 브라우저의 모든 프로필). 브라우저 이름 왼쪽의 [전체선택]으로 한 번에 끄고 켤 수 있습니다.')
STEP(3, '브라우저가 실행 중이면 안내에 따라 잠시 닫았다가 다시 열립니다 — 북마크바 맨 앞에 [🛒품의캡처]가 추가됩니다.')
BOX('안내', [
  '· 크롬 북마크 동기화가 켜져 있으면 북마크가 바의 맨 뒤에 생길 수 있습니다 — 클릭 후 편리한 곳으로 드래그하세요.',
  '· 이미 같은 북마크가 있으면 중복 만들지 않고 제자리로 이동시킵니다.',
  '· 등록 후 브라우저를 다시 열어도 사라지지 않도록 여러 단계의 안전장치가 동작합니다.'
])
doc.moveDown(0.2)

H1('3. 브라우저 확장(품의캡처) 설치')
P('확장 프로그램을 쓰면 북마크릿보다 안정적으로 캡처할 수 있습니다(특히 네이버 장바구니는 확장 사용을 권장합니다).')
H2('3.1 자동 설치(권장)')
STEP(1, '프로그램 상단의 [🧩 익스텐션 추가] 버튼을 클릭합니다 — 자동 설치 도구가 바로 열립니다.')
STEP(2, '도구 창에서 설치할 브라우저·프로필을 선택합니다.')
STEP(3, '[개발자 모드 켜기 + 익스텐션 설치] 버튼을 누르면 브라우저 종료 → 개발자 모드 켜기 → 품의캡처 확장 자동 로드까지 한 번에 진행됩니다.')
P('설치 후 브라우저 확장 목록(오른쪽 위 퍼즐 아이콘)에서 "품의캡처"가 켜져 있으면 완료입니다.')
H2('3.2 수동 설치(자동 설치가 어려운 경우)')
STEP(1, '브라우저를 직접 실행하고 주소창에 확장 페이지 주소를 입력합니다 — 크롬: chrome://extensions / Edge: edge://extensions / 웨일: whale://extensions')
STEP(2, '페이지에서 "개발자 모드"를 켭니다.')
STEP(3, '"압축해제된 확장 프로그램 로드"를 클릭하고, 프로그램 [🧩 익스텐션 추가] 창에 표시되는 확장 폴더(…\\자동 품의 요구 생성기\\extension)를 선택합니다.')
P('※ 최신 크롬은 정책상 프로그램이 확장을 직접 로드하는 것을 막으므로, 크롬은 위 수동 방식 또는 자동 설치 도구를 이용해 주세요.')
doc.moveDown(0.2)

H1('4. 기본 사용 흐름')
STEP(1, '브라우저에서 쇼핑몰 로그인 후 주문서(또는 장바구니) 화면을 엽니다.')
STEP(2, '장바구니라면 주문할 상품에 체크(V)합니다 — 체크한 상품만 추출됩니다.')
STEP(3, '익스텐션 아이콘 또는 북마크릿 [🛒품의캡처]를 누릅니다 — 프로그램이 자동으로 받아 왼쪽에 표시하고, 오른쪽 표에 품목을 채웁니다.')
STEP(4, '오른쪽 표에서 품목을 확인·수정합니다 — 셀을 클릭해 편집하고, Tab/Shift+Tab으로 다음·이전 셀로 이동합니다. 셀을 선택한 상태에서 [＋ 행 추가]를 누르면 그 행 바로 아래에 새 행이 생기고(품목명 입력 대기), 선택이 없으면 맨 아래에 추가됩니다. 행 추가/삭제, 중복 추출분 일괄 삭제 [중복삭제] 버튼도 사용할 수 있습니다.')
STEP(5, '필요하면 상태바 우측에서 단가 인상 %를 선택합니다(예상단가에만 적용, 배송비 제외). 상태바 우측에는 품목 수·총액·단가·[엑셀에 저장]이 모여 있습니다.')
STEP(6, '[엑셀에 저장]으로 저장합니다 — 선택한 엑셀 파일이 현재 표 내용으로 교체되며 .bak 백업이 자동 생성됩니다. 누적하려면 [기존 엑셀 불러오기] → 표 추가 → [엑셀에 저장] 순서로 진행합니다.')
BOX('알아두기', [
  '· 주문서가 아닌 화면을 캡처하면 "주문서 상태에서 품의캡처 눌러 주세요" 같은 안내 팝업이 뜹니다 — 올바른 화면에서 다시 눌러 주세요.',
  '· 브라우저 저장 특성상 체크한 일부 상품이 캡처에 누락되면 "체크 N개 중 M개만 저장…" 경고가 뜹니다 — 장바구니를 끝까지 스크롤한 뒤 품의캡처로 다시 담아 주세요.',
  '· 같은 장바구니를 두 번 추출하면 표에 [중복삭제] 버튼이 나타나 한 세트만 남기고 정리할 수 있습니다.'
])
doc.moveDown(0.2)

H1('5. 미리보기(왼쪽 화면) 조작')
P('· 마우스 휠: 확대/축소 (커서 위치 기준, 25~300% — 감도는 설정에서 조절)')
P('· 오른쪽 클릭 또는 휠 버튼 드래그: 화면 잡아끄기(pan)')
P('· 뷰어 도구 모음: [＋ 확대] [－ 축소] [100%] 버튼, 네이버·쿠팡 장바구니는 [카드/원본] 보기 전환')
P('· [▣ 반반] 버튼(상단): 미리보기를 숨기고 프로그램 창을 화면 오른쪽 정확히 절반에 맞춥니다 — 실행하면 왼쪽에 놓을 프로그램을 고르는 화면(스냅 애시스트)이 나타나면 품의 결재 시스템 등을 선택하세요. 다시 누르면 미리보기가 돌아옵니다.')
doc.moveDown(0.2)

H1('6. 설정(⚙) 메뉴')
P('· 북마크바 추가 / 익스텐션 추가 / 반반 버튼 표시: 화면 상단 버튼 3종을 켜거나 끕니다(기본: 켜기)')
P('· 새 쇼핑몰 규칙 추가 버튼 표시: 학습용 버튼을 상단에 나타냅니다(기본: 끄기)')
P('· 휠 줌 감도: 미리보기 휠 줌 속도 (기본 1.7%/노치)')
P('· 추출 결과 글자 크기: 표 글자 크기 (기본 150%) — 머리글 경계를 드래그해 열 폭도 조절')
P('· 시작 프로그램: Windows 시작 시 자동 실행 등록/삭제')
P('· 캡처 파일 보관 기간: 프로그램이 캡처 원본을 보관하는 기간 (기본 5일, 0 = 끔)')
P('· 쇼핑몰 규칙 업데이트: [⬇ 업데이트 확인]으로 최신 쇼핑몰 규칙을 자동 받아 적용합니다. 열었던 화면에 새 규칙을 적용하려면 뷰어 위 규칙 선택으로 다시 선택하세요.')
doc.moveDown(0.2)

H1('7. 새 쇼핑몰 규칙 만들기(매핑 학습)')
P('목록에 없는 쇼핑몰은 화면을 클릭하는 것만으로 나만의 규칙을 만들 수 있습니다. 이 기능은 설정의 "새 쇼핑몰 규칙 추가 버튼 표시"를 켜면 상단에 버튼이 나타납니다.')
STEP(1, '[새 쇼핑몰 규칙 추가] 버튼 → 캡처 대기 상태에서 그 쇼핑몰 장바구니/주문서 화면을 품의캡처로 보냅니다.')
STEP(2, '안내에 따라 상품이 2개 이상 보이게 한 뒤, 각 상품의 행(또는 열)을 순서대로 클릭합니다.')
STEP(3, '상품명 → 수량 → 주문금액 → 배송비 순으로 각각 클릭해 지정합니다.')
STEP(4, '규칙 이름을 확인하고 저장하면 바로 적용됩니다. 잘못 클릭한 항목은 ✕로 지우고 다시 클릭할 수 있습니다.')
doc.moveDown(0.2)

H1('8. 자주 묻는 문제')
BOX('Q. "주문서 상태에서 품의캡처 눌러 주세요"라는 팝업이 뜹니다', [
  '· 그 쇼핑몰은 장바구니가 아닌 주문/결제 화면에서 캡처해야 합니다. 주문서 화면까지 진행한 뒤 다시 눌러 주세요.',
  '· 반대로 장바구니 전용 안내가 뜨는 몰(네이버·쿠팡 등)은 장바구니 화면에서 체크 후 캡처합니다.'
])
BOX('Q. 네이버 장바구니 캡처가 비어 있거나 상품이 누락됩니다', [
  '· 네이버 장바구니는 확장 프로그램(품의캡처 아이콘) 사용을 권장합니다 — 확장이 화면을 자동으로 축소해 전체 상품을 담습니다.',
  '· 확장이 오래되면 캡처가 거부될 수 있습니다 — 브라우저를 완전히 재시작하거나 [🧩 익스텐션 추가]로 재설치해 주세요.'
])
BOX('Q. 무료배송인데 배송비가 표에 나타납니다 / 또는 배송비가 없습니다', [
  '· 배송비는 화면에 표시된 기준을 따릅니다. 예: 다이소몰은 상품 합계 30,000원 이상이면 무료 — 기준을 채우면 배송비 행이 만들어지지 않고, 미달이면 표시된 배송비가 추가됩니다.'
])
BOX('Q. 저장한 엑셀에 이전 내용이 사라졌습니다', [
  '· [엑셀에 저장]은 현재 표 내용으로 파일을 "교체"하는 방식입니다(원본은 .bak으로 백업). 이어서 누적하려면 [기존 엑셀 불러오기]로 불러온 뒤 캡처를 추가하고 저장하세요.'
])
BOX('Q. 화면 상단에 "MHTML 폴더/파일 열기" 버튼이 없습니다', [
  '· 기본적으로 숨겨져 있으며, 표시 여부는 관리자 설정에서만 바꿀 수 있습니다.',
  '· 캡처는 🛒품의캡처 북마크릿·익스텐션으로 진행하므로 일반 사용은 버튼이 없어도 문제없습니다.'
])
doc.moveDown(0.2)

H1('9. 한 페이지 요약')
BOX('사용 요약', [
  '1. 최초 1회: [⭐ 북마크바 추가] 또는 [🧩 익스텐션 추가]로 캡처 수단을 설치한다.',
  '2. 쇼핑몰에서 주문서(또는 장바구니 체크 후) 화면을 연다.',
  '3. 🛒품의캡처(아이콘/북마크)를 누른다 — 품목이 자동으로 표에 채워진다.',
  '4. 표를 확인·수정하고(필요 시 단가 인상 % 선택) [엑셀에 저장]을 누른다.',
  '5. 모르는 쇼핑몰은 [새 쇼핑몰 규칙 추가]로 클릭 학습, 새 규칙은 [⬇ 업데이트 확인]으로 자동 받는다.'
])

doc.end()
console.log('PDF written:', out)
